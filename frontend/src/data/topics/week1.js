// Topic content for Week 1 — Java OOP Depth + Git + Maven + Testing Habit

const WEEK1_TOPICS = {
  w1d1: {
    id: "w1d1", weekId: "w1", dayNumber: 1, dayLabel: "Monday", estimatedMinutes: 90,
    title: "Git Workflow + Maven Basics",
    subtitle: "Project: repository initialised",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Git — what you must understand:" },
      { type: "bullet", value: "What git is actually for — not backup, but communication between engineers over time" },
      { type: "bullet", value: "What an atomic commit is — one logical change, one commit, one clear message" },
      { type: "bullet", value: "Conventional commits format — type: description — and what each type means: feat, fix, refactor, chore, test, docs" },
      { type: "bullet", value: "Why commit history matters — senior engineers read history before reading code" },
      { type: "bullet", value: "What git rebase -i does — combining messy local commits before pushing to remote" },
      { type: "bullet", value: "What HEAD~3 means — referring to the last 3 commits relative to current position" },
      { type: "bullet", value: "Why squashing exists — keeping history readable and meaningful for the team" },
      { type: "empty", value: "" },
      { type: "bold", value: "Maven — what you must understand:" },
      { type: "bullet", value: "What Maven actually does — dependency management, build lifecycle management, packaging" },
      { type: "bullet", value: "What each dependency scope means:" },
      { type: "bullet", value: "  - compile — default. Available everywhere including the production jar." },
      { type: "bullet", value: "  - test — JUnit and Mockito go here. NOT included in the production jar." },
      { type: "bullet", value: "  - provided — container (e.g. Tomcat) provides it at runtime, not packaged." },
      { type: "bullet", value: "  - runtime — not needed to compile, but needed at runtime." },
      { type: "bullet", value: "What a transitive dependency is — when you add spring-boot-starter-web, it pulls in Tomcat, Jackson, Spring MVC and many more" },
      { type: "bullet", value: "What mvn dependency:tree shows — every dependency and exactly where it came from" },
      { type: "bullet", value: "What a fat jar is — Spring Boot packages ALL dependencies inside the jar as a zip file" },
      { type: "bullet", value: "Why the fat jar runs anywhere — no separate classpath or installation needed" },
      { type: "bullet", value: "What BOOT-INF/lib/ contains — every dependency jar bundled inside your jar" },
      { type: "tip", value: "When a NoClassDefFoundError hits in production, mvn dependency:tree is how you find the cause. This is a real debugging tool." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "20 mins" },
      { type: "p", value: "Git is not a backup tool. It is a communication tool. Every commit tells the next engineer — including yourself in 3 months — what changed and why. Senior engineers read commit history before reading code. Start correctly." },
      { type: "exercise", label: "45 mins — Git setup" },
      { type: "code", lang: "bash", lines: ["# Initialise the project","git init payment-wallet","cd payment-wallet","git commit --allow-empty -m \"chore: initialise repository\""] },
      { type: "p", value: "From today every commit message follows this exact format:" },
      { type: "code", lang: "text", lines: ["type: what changed in one sentence"] },
      { type: "p", value: "Types: feat (new feature), fix (bug fix), refactor (restructure without behaviour change), chore (config/tooling), test (adding or fixing tests), docs (README or comments)." },
      { type: "bold", value: "Atomic commits — one logical change per commit:" },
      { type: "code", lang: "bash", lines: ["# Wrong — do not do this","git commit -m \"added payment classes and tests and fixed bug and updated pom\"","","# Right — one thing per commit","git commit -m \"feat: add UPIPayment strategy class\"","git commit -m \"test: add UPIPayment unit tests\""] },
      { type: "bold", value: "Squashing — combine messy local commits before pushing:" },
      { type: "code", lang: "bash", lines: ["git rebase -i HEAD~3","# In the editor: change 'pick' to 'squash' on commits to merge","# The top commit stays as 'pick' — it becomes the final commit"] },
      { type: "bold", value: "Failure simulation — what bad history looks like:" },
      { type: "code", lang: "bash", lines: ["git commit -m \"fix\"","git commit -m \"fix2\"","git commit -m \"asdf\"","git log --oneline","# This is what juniors push. Squash it before it reaches GitHub.","git rebase -i HEAD~3   # squash all three into one meaningful commit"] },
      { type: "exercise", label: "25 mins — Maven basics" },
      { type: "p", value: "Create a Spring Boot project at start.spring.io — Java 21, Maven, Spring Web, Spring Data JPA, MySQL Driver, Spring Security, Lombok, Validation. Download and open in IntelliJ." },
      { type: "bold", value: "Understand dependency scopes — read your pom.xml:" },
      { type: "code", lang: "xml", lines: ["<dependency>","  <scope>compile</scope>   <!-- default: available everywhere -->","  <scope>test</scope>      <!-- JUnit, Mockito go here — not in production jar -->","  <scope>provided</scope>  <!-- container provides it at runtime -->","  <scope>runtime</scope>   <!-- not needed to compile, needed to run -->","</dependency>"] },
      { type: "bold", value: "Run this and read every line of output:" },
      { type: "code", lang: "bash", lines: ["mvn dependency:tree","# This shows every transitive dependency — where versions come from","# When you get a NoClassDefFoundError in production, this is how you debug it"] },
      { type: "bold", value: "Build the jar and look inside:" },
      { type: "code", lang: "bash", lines: ["mvn clean package -DskipTests","cd target","unzip -l payment-wallet-0.0.1-SNAPSHOT.jar | head -40","# A jar is a zip file. Spring Boot fat jar contains all dependencies inside.","# BOOT-INF/lib/ contains every dependency jar."] },
      { type: "bold", value: "Failure simulation — wrong scope:" },
      { type: "code", lang: "bash", lines: ["# Move spring-boot-starter-web from compile to test scope","# Run: mvn clean package","# Try to run the jar: java -jar target/payment-wallet.jar","# Observe: ClassNotFoundException at startup","# Restore compile scope. Understand why scope matters."] },
      { type: "done", value: "Project is on GitHub. You explain dependency scope, read a dependency tree, and explain what a fat jar contains." },
    ],
  },
  w1d2: {
    id: "w1d2", weekId: "w1", dayNumber: 2, dayLabel: "Tuesday", estimatedMinutes: 90,
    title: "Interfaces vs Abstract Classes + Strategy Pattern",
    subtitle: "Project: payment/PaymentStrategy.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Interfaces vs Abstract Classes — what you must understand:" },
      { type: "bullet", value: "What an interface is — a contract defining capabilities, not implementation or data" },
      { type: "bullet", value: "What an abstract class is — partial implementation that cannot be instantiated directly" },
      { type: "bullet", value: "What changed in Java 8 — default methods added to interfaces for backward compatibility" },
      { type: "bullet", value: "What changed in Java 9 — private methods inside interfaces for shared internal logic" },
      { type: "bullet", value: "What \"instance state\" means — fields that belong to a specific object instance (e.g. upiId, maskedCard)" },
      { type: "bullet", value: "Why interfaces CANNOT hold instance state — an interface is a contract shared by all implementors. State belongs to instances, not contracts." },
      { type: "bullet", value: "Why abstract classes CAN hold instance state — they represent a base object with shared data" },
      { type: "bullet", value: "Multiple inheritance — Java allows implementing multiple interfaces but extending only one class. Why this is safe for interfaces but dangerous for classes." },
      { type: "bullet", value: "The one real post-Java-8 rule: if you need instance state, use abstract class. Otherwise, prefer interface." },
      { type: "empty", value: "" },
      { type: "bold", value: "Strategy Pattern — what you must understand:" },
      { type: "bullet", value: "What problem Strategy solves — eliminates if-else chains that grow every time a new payment type is added" },
      { type: "bullet", value: "What the three parts of Strategy are: interface (strategy contract), concrete classes (UPI, Card), context (PaymentContext which holds and runs the strategy)" },
      { type: "bullet", value: "Why Strategy satisfies OCP — you add new payment types without ever modifying PaymentService" },
      { type: "bullet", value: "How Strategy enables testability — you can pass a mock strategy in tests instead of making a real UPI network call" },
      { type: "bullet", value: "Why UPIPayment implements the interface rather than extending a class — it has its own instance state (upiId) that no other payment type shares" },
      { type: "tip", value: "The connection to testing: code depending on an interface can be tested with a mock. Code depending on a concrete class cannot. This is why Strategy matters." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "40 mins" },
      { type: "p", value: "The real post-Java-8 answer is one word: state. An interface cannot hold instance state. An abstract class can. Everything else — default methods, multiple inheritance — is secondary to this line." },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/payment/PaymentStrategy.java","public interface PaymentStrategy {","    void processPayment(double amount);","    // Java 8 default method — backward compatibility, not state","    default void logPayment(double amount) {","        System.out.println(\"Processing payment: \" + amount);","    }","    // Java 9 private method — shared logic inside interface","    private void validate(double amount) {","        if (amount <= 0) throw new IllegalArgumentException(\"Amount must be positive\");","    }","}"] },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/payment/UPIPayment.java","public class UPIPayment implements PaymentStrategy {","    private final String upiId; // STATE — why this cannot be in the interface","    public UPIPayment(String upiId) { this.upiId = upiId; }","    @Override","    public void processPayment(double amount) {","        System.out.println(\"UPI \" + upiId + \": processing \" + amount);","        logPayment(amount);","    }","}"] },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/payment/CardPayment.java","public class CardPayment implements PaymentStrategy {","    private final String maskedCard; // STATE","    private final String cardType;   // STATE","    public CardPayment(String maskedCard, String cardType) {","        this.maskedCard = maskedCard;","        this.cardType = cardType;","    }","    @Override","    public void processPayment(double amount) {","        System.out.println(cardType + \" \" + maskedCard + \": processing \" + amount);","    }","}"] },
      { type: "bold", value: "Failure simulation — try to add state to the interface:" },
      { type: "code", lang: "java", lines: ["// Inside PaymentStrategy interface, add:","private String processorId = \"DEFAULT\"; // try this","// Compile. Read the exact error message.","// Now you have seen why the rule exists — not just read about it."] },
      { type: "exercise", label: "50 mins — First JUnit tests" },
      { type: "code", lang: "java", lines: ["// File: src/test/java/com/wallet/payment/UPIPaymentTest.java","class UPIPaymentTest {","    private UPIPayment payment;","","    @BeforeEach","    void setUp() { payment = new UPIPayment(\"user@upi\"); }","","    @Test","    void processPayment_validAmount_doesNotThrow() {","        assertDoesNotThrow(() -> payment.processPayment(500.0));","    }","","    @Test","    void processPayment_zeroAmount_shouldBeHandled() {","        assertDoesNotThrow(() -> payment.processPayment(0.0));","    }","}"] },
      { type: "done", value: "PaymentStrategy, UPIPayment, CardPayment exist in the project. 2 tests are green. You explain why interface cannot hold state." },
    ],
  },
  w1d3: {
    id: "w1d3", weekId: "w1", dayNumber: 3, dayLabel: "Wednesday", estimatedMinutes: 90,
    title: "SOLID Principles — Applied to payment-wallet",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Why SOLID exists — before learning the rules:" },
      { type: "bullet", value: "The real problem SOLID solves — code that is hard to change, hard to test, and breaks unexpectedly when modified" },
      { type: "bullet", value: "How SOLID violations show up in code review — the exact vocabulary senior engineers use: \"this class has too many responsibilities\", \"this is tightly coupled\", \"this cannot be tested\"" },
      { type: "empty", value: "" },
      { type: "bold", value: "SRP — Single Responsibility Principle:" },
      { type: "bullet", value: "What \"responsibility\" actually means — a reason to change, not just \"does one thing\"" },
      { type: "bullet", value: "Why multiple responsibilities in one class cause problems — two different teams can conflict editing the same file" },
      { type: "bullet", value: "How to identify an SRP violation — ask \"how many different reasons could make me change this class?\"" },
      { type: "bullet", value: "Project example — PaymentService doing payment logic AND email AND logging is 3 responsibilities, 3 reasons to change" },
      { type: "empty", value: "" },
      { type: "bold", value: "OCP — Open/Closed Principle:" },
      { type: "bullet", value: "What \"open for extension, closed for modification\" means in plain language" },
      { type: "bullet", value: "How your PaymentStrategy interface already implements OCP — adding NetBanking payment never requires touching PaymentService" },
      { type: "bullet", value: "What an OCP violation looks like — a switch or if-else block that must grow every time a new type is added" },
      { type: "empty", value: "" },
      { type: "bold", value: "LSP — Liskov Substitution Principle:" },
      { type: "bullet", value: "What \"honouring the contract\" means — every implementation must behave as the interface promises for all valid inputs" },
      { type: "bullet", value: "What an LSP violation looks like — a subclass that throws exceptions for inputs the interface declares are valid" },
      { type: "bullet", value: "Why LSP violations break caller code — callers trust the abstraction and cannot know which implementation will behave correctly" },
      { type: "empty", value: "" },
      { type: "bold", value: "ISP — Interface Segregation Principle:" },
      { type: "bullet", value: "What a \"fat interface\" is — forces implementations to provide methods they do not use" },
      { type: "bullet", value: "Why fat interfaces create silent bugs — implementations must throw UnsupportedOperationException or return dummy values" },
      { type: "bullet", value: "How to split interfaces — by what each specific caller actually needs" },
      { type: "empty", value: "" },
      { type: "bold", value: "DIP — Dependency Inversion Principle:" },
      { type: "bullet", value: "What \"depend on abstraction, not concrete class\" means and why it matters" },
      { type: "bullet", value: "Why depending on UPIPayment directly makes PaymentService untestable — you cannot replace a hardcoded dependency with a mock" },
      { type: "bullet", value: "How constructor injection enables DIP — the interface is passed in from outside, not created inside the class" },
      { type: "bullet", value: "Why DIP is the principle that makes mocking and unit testing possible at all" },
      { type: "tip", value: "SOLID is interview vocabulary. Know all 5 principles using your own project as the example, not textbook definitions." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "30 mins" },
      { type: "p", value: "SOLID is not academic. It is the vocabulary senior engineers use in code review. When your code violates SRP, a senior will say 'this class has too many responsibilities.' Know what that means before they say it." },
      { type: "bold", value: "SRP — Single Responsibility Principle:" },
      { type: "code", lang: "java", lines: ["// WRONG — PaymentService doing too many things","class PaymentService {","    void processPayment() { }   // payment logic","    void sendEmail() { }        // notification — separate responsibility","    void writeLog() { }         // logging — separate responsibility","    void validateUser() { }     // auth — separate responsibility","}","// RIGHT — one class, one reason to change","class PaymentService    { void processPayment() { } }","class NotificationService { void sendEmail() { } }","class PaymentValidator  { void validateUser() { } }"] },
      { type: "bold", value: "OCP — Open/Closed Principle — your PaymentStrategy already does this:" },
      { type: "code", lang: "java", lines: ["// Adding NetBanking payment does NOT modify PaymentService","// It EXTENDS by adding a new class: NetBankingPayment implements PaymentStrategy","// PaymentService is closed for modification, open for extension"] },
      { type: "bold", value: "LSP — Liskov Substitution:" },
      { type: "code", lang: "java", lines: ["// Every PaymentStrategy implementation must honour the contract","// If UPIPayment.processPayment() throws for valid amounts — LSP violation","// Callers of PaymentStrategy cannot trust the abstraction"] },
      { type: "bold", value: "ISP — Interface Segregation:" },
      { type: "code", lang: "java", lines: ["// WRONG — fat interface","interface PaymentProcessor {","    void processPayment(double amount);","    void refund(double amount);          // not all processors support refund","    void generateStatement();            // not payment-related","}","// RIGHT — split by what callers actually need","interface PaymentProcessor { void processPayment(double amount); }","interface Refundable        { void refund(double amount); }"] },
      { type: "bold", value: "DIP — Dependency Inversion:" },
      { type: "code", lang: "java", lines: ["// WRONG — high level depends on low level","class PaymentService {","    private UPIPayment payment = new UPIPayment(\"id\"); // concrete","}","// RIGHT — depend on abstraction","class PaymentService {","    private final PaymentStrategy payment; // interface","    public PaymentService(PaymentStrategy payment) { this.payment = payment; }","}"] },
      { type: "exercise", label: "60 mins — Refactor existing code to follow SOLID" },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/service/PaymentService.java","public class PaymentService {","    private final PaymentStrategy strategy;","    public PaymentService(PaymentStrategy strategy) {","        this.strategy = strategy;","    }","    public void executePayment(double amount) {","        strategy.processPayment(amount);","    }","}"] },
      { type: "bold", value: "Failure simulation — violate DIP and feel the pain:" },
      { type: "code", lang: "java", lines: ["// Change PaymentService to new UPIPayment() directly","// Now try to test PaymentService without making a real UPI call","// You cannot — the dependency is hardcoded","// Restore the interface dependency. Now you can pass a mock in tests."] },
      { type: "done", value: "You apply all 5 SOLID principles to real classes in the project. You explain each one using payment-wallet as the example, not a textbook definition." },
    ],
  },
  w1d4: {
    id: "w1d4", weekId: "w1", dayNumber: 4, dayLabel: "Thursday", estimatedMinutes: 90,
    title: "Design Patterns: Strategy (complete) + Factory",
    subtitle: "Project: payment/PaymentContext.java + payment/PaymentFactory.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Strategy Pattern — completing your understanding:" },
      { type: "bullet", value: "What PaymentContext is and why it exists — the holder of the current strategy, delegates execution to it" },
      { type: "bullet", value: "What runtime switching means — user can switch from UPI to Card within the same session without app restart" },
      { type: "bullet", value: "Why a null strategy check is necessary — defensive programming: fail fast with a clear error instead of a NullPointerException deep in the stack" },
      { type: "empty", value: "" },
      { type: "bold", value: "Factory Pattern — what you must understand:" },
      { type: "bullet", value: "What problem Factory solves — the caller should not need to know which concrete class to instantiate" },
      { type: "bullet", value: "What \"factory decides which class to create\" means — based on runtime input, not compile-time hardcoding" },
      { type: "bullet", value: "Why a static factory method is common — no need to instantiate the factory itself, simpler to call" },
      { type: "bullet", value: "Why the default/unknown case must always be handled explicitly — missing it causes runtime MatchException with no useful message to the caller" },
      { type: "bullet", value: "How Factory and Strategy work together in your project — Factory creates the right strategy, Context holds and runs it" },
      { type: "tip", value: "Interview question: what is the difference between Factory and Strategy? Factory decides what to create. Strategy decides how to behave at runtime." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "30 mins" },
      { type: "p", value: "Strategy pattern: define a family of algorithms, encapsulate each one, make them interchangeable. Your PaymentStrategy interface is already the strategy. PaymentContext holds the current strategy and delegates to it." },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/payment/PaymentContext.java","public class PaymentContext {","    private PaymentStrategy strategy;","","    public PaymentContext(PaymentStrategy strategy) {","        this.strategy = strategy;","    }","","    public void setStrategy(PaymentStrategy strategy) {","        this.strategy = strategy;","    }","","    public void executePayment(double amount) {","        if (strategy == null) throw new IllegalStateException(\"No payment strategy set\");","        strategy.processPayment(amount);","    }","}"] },
      { type: "p", value: "Factory pattern: create objects without specifying the exact class. Caller says what it wants, factory decides which class to instantiate." },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/payment/PaymentFactory.java","public class PaymentFactory {","    public static PaymentStrategy create(String type, String identifier) {","        return switch (type.toUpperCase()) {","            case \"UPI\"  -> new UPIPayment(identifier);","            case \"CARD\" -> new CardPayment(identifier, \"VISA\");","            default -> throw new IllegalArgumentException(\"Unknown payment type: \" + type);","        };","    }","}"] },
      { type: "bold", value: "Failure simulation — no default in factory:" },
      { type: "code", lang: "java", lines: ["// Remove the default case from the switch","// Call PaymentFactory.create(\"CRYPTO\", \"addr\")","// Observe: no compile error, runtime MatchException","// Restore the default with a clear exception message"] },
      { type: "exercise", label: "60 mins — Tests for Context and Factory" },
      { type: "code", lang: "java", lines: ["// File: src/test/java/com/wallet/payment/PaymentContextTest.java","class PaymentContextTest {","","    @Test","    void executePayment_withUPIStrategy_doesNotThrow() {","        PaymentContext ctx = new PaymentContext(new UPIPayment(\"a@upi\"));","        assertDoesNotThrow(() -> ctx.executePayment(100.0));","    }","","    @Test","    void executePayment_strategySwitch_usesNewStrategy() {","        PaymentContext ctx = new PaymentContext(new UPIPayment(\"a@upi\"));","        ctx.setStrategy(new CardPayment(\"**** 1234\", \"VISA\"));","        assertDoesNotThrow(() -> ctx.executePayment(200.0));","    }","","    @Test","    void executePayment_noStrategy_throwsIllegalState() {","        PaymentContext ctx = new PaymentContext(null);","        assertThrows(IllegalStateException.class, () -> ctx.executePayment(100.0));","    }","}"] },
      { type: "done", value: "PaymentContext and PaymentFactory exist and are tested. You explain Strategy pattern using your own code, not a definition." },
    ],
  },
  w1d5: {
    id: "w1d5", weekId: "w1", dayNumber: 5, dayLabel: "Friday", estimatedMinutes: 90,
    title: "Design Patterns: Singleton + Builder",
    subtitle: "Project: JwtTokenProvider.java + PaymentRequest builder",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Singleton Pattern — what you must understand:" },
      { type: "bullet", value: "What Singleton guarantees — only one instance of the class exists in the entire JVM" },
      { type: "bullet", value: "Real use cases — config holders, connection pools, JWT token providers, thread pools" },
      { type: "bullet", value: "Why the naive version is broken under concurrency — Thread A and B both see null, both create separate instances" },
      { type: "bullet", value: "What volatile does in this context — prevents the JVM from reordering the write to the instance field before the constructor finishes" },
      { type: "bullet", value: "Why reordering is dangerous — Thread B can see a non-null reference to a partially constructed object and use it" },
      { type: "bullet", value: "What double-checked locking is — first check without lock (fast path), second check inside synchronized (safe path)" },
      { type: "bullet", value: "Why both checks are needed — first check avoids acquiring the lock on every call. Second check prevents duplicate creation under concurrent access." },
      { type: "empty", value: "" },
      { type: "bold", value: "Builder Pattern — what you must understand:" },
      { type: "bullet", value: "What problem Builder solves — telescoping constructors with many parameters become unreadable and error-prone" },
      { type: "bullet", value: "What telescoping constructors look like — new PaymentRequest(\"u1\",\"u2\",500.0,\"INR\",\"\",\"\") — which parameter is which?" },
      { type: "bullet", value: "How Builder separates required from optional fields — required fields in Builder constructor, optional fields as named fluent methods" },
      { type: "bullet", value: "What method chaining (fluent API) means — each setter returns \"this\" so calls can be chained: .currency(\"INR\").description(\"rent\")" },
      { type: "bullet", value: "Why the resulting object is immutable — private constructor, all fields final, only Builder can create it" },
      { type: "bullet", value: "Where Lombok @Builder saves time — generates all this boilerplate automatically in Spring Boot projects" },
      { type: "tip", value: "PaymentRequest is the perfect Builder use case — 3 required fields and 3+ optional fields. The readability difference vs a constructor is obvious." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "35 mins" },
      { type: "p", value: "Singleton: only one instance of a class exists in the JVM. Common use: config holders, connection pools, token providers. The naive version is broken under concurrency. The correct version uses volatile + double-checked locking." },
      { type: "code", lang: "java", lines: ["// WRONG — not thread safe","public class JwtTokenProvider {","    private static JwtTokenProvider instance;","    public static JwtTokenProvider getInstance() {","        if (instance == null) {              // Thread A and B both see null","            instance = new JwtTokenProvider(); // both create instances","        }","        return instance; // two different instances returned","    }","}"] },
      { type: "code", lang: "java", lines: ["// CORRECT — volatile + double-checked locking","// File: src/main/java/com/wallet/config/JwtTokenProvider.java","public class JwtTokenProvider {","    private static volatile JwtTokenProvider instance;","    private final String secretKey;","","    private JwtTokenProvider() {","        this.secretKey = \"your-256-bit-secret-key-for-hs256-algorithm\";","    }","","    public static JwtTokenProvider getInstance() {","        if (instance == null) {                      // first check — no lock","            synchronized (JwtTokenProvider.class) {  // lock only if needed","                if (instance == null) {              // second check — under lock","                    instance = new JwtTokenProvider();","                }","            }","        }","        return instance;","    }","","    public String generateToken(String userId) {","        return \"token-\" + userId + \"-\" + System.currentTimeMillis();","    }","}"] },
      { type: "p", value: "Why volatile here: without volatile, the JVM can reorder the write to instance before the constructor finishes. Thread B sees a non-null but partially constructed object. volatile prevents this reordering." },
      { type: "p", value: "Builder pattern: construct complex objects step by step. Useful when a class has many optional fields and telescoping constructors become unreadable." },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/dto/PaymentRequest.java","public class PaymentRequest {","    private final String fromUserId;","    private final String toUserId;","    private final double amount;","    private final String currency;","    private final String idempotencyKey;","    private final String description;","","    private PaymentRequest(Builder builder) {","        this.fromUserId     = builder.fromUserId;","        this.toUserId       = builder.toUserId;","        this.amount         = builder.amount;","        this.currency       = builder.currency;","        this.idempotencyKey = builder.idempotencyKey;","        this.description    = builder.description;","    }","","    public static class Builder {","        private final String fromUserId;  // required","        private final String toUserId;    // required","        private final double amount;      // required","        private String currency     = \"INR\";","        private String idempotencyKey = \"\";","        private String description  = \"\";","","        public Builder(String fromUserId, String toUserId, double amount) {","            this.fromUserId = fromUserId;","            this.toUserId   = toUserId;","            this.amount     = amount;","        }","        public Builder currency(String c)      { this.currency = c; return this; }","        public Builder idempotencyKey(String k){ this.idempotencyKey = k; return this; }","        public Builder description(String d)   { this.description = d; return this; }","        public PaymentRequest build()          { return new PaymentRequest(this); }","    }","}"] },
      { type: "bold", value: "Failure simulation — remove volatile from Singleton:" },
      { type: "code", lang: "java", lines: ["// Remove volatile keyword from instance field","// Run 1000 threads all calling getInstance() simultaneously","// Under high concurrency you may get multiple instances","// The bug is intermittent — this is why concurrency bugs are hard","// Restore volatile. The second check inside synchronized is also required."] },
      { type: "done", value: "JwtTokenProvider is a correct thread-safe Singleton. PaymentRequest uses Builder. You explain why volatile is required in double-checked locking." },
    ],
  },
  w1d6: {
    id: "w1d6", weekId: "w1", dayNumber: 6, dayLabel: "Saturday", estimatedMinutes: 150,
    title: "Observer Pattern + Week 1 Integration",
    subtitle: "Project: observer/ package",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Observer Pattern — what you must understand:" },
      { type: "bullet", value: "What problem Observer solves — when one event must notify multiple independent handlers without the source knowing who they are" },
      { type: "bullet", value: "The wallet example: payment success must trigger audit log, fraud check, and notification. Without Observer, PaymentService calls all three directly — tight coupling." },
      { type: "bullet", value: "Why direct coupling is a problem — adding a fourth handler requires modifying PaymentService. OCP violated every time." },
      { type: "bullet", value: "What the three parts of Observer are: subject/publisher (fires events), observer interface (contract for handlers), concrete observers (AuditLogger, FraudDetector)" },
      { type: "bullet", value: "How Observer satisfies OCP — adding a new observer never requires modifying PaymentEventPublisher" },
      { type: "bullet", value: "How Observer relates to the real world — Spring ApplicationEvents, Kafka consumers, and webhook notification systems all use this same pattern" },
      { type: "bullet", value: "What unsubscribe is for — removing observers when no longer needed, preventing memory leaks" },
      { type: "empty", value: "" },
      { type: "bold", value: "Week 1 integration — what to verify:" },
      { type: "bullet", value: "Every class built this week exists in the correct package inside payment-wallet" },
      { type: "bullet", value: "Every class has at least one corresponding test that is green" },
      { type: "bullet", value: "All commit messages follow the type: description format without exception" },
      { type: "bullet", value: "No WIP, fix, fix2, or asdf commits anywhere in the GitHub history" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "40 mins" },
      { type: "p", value: "Observer pattern: when one object changes state, all dependents are notified automatically. In your wallet: when a payment succeeds, the notification service, the audit logger, and the fraud detector all need to know. Without Observer, PaymentService calls each one directly — tight coupling." },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/observer/PaymentObserver.java","public interface PaymentObserver {","    void onPaymentProcessed(String userId, double amount, String status);","}"] },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/observer/AuditLogger.java","public class AuditLogger implements PaymentObserver {","    private static final Logger log = LoggerFactory.getLogger(AuditLogger.class);","    @Override","    public void onPaymentProcessed(String userId, double amount, String status) {","        log.info(\"AUDIT userId={} amount={} status={}\", userId, amount, status);","    }","}"] },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/observer/FraudDetector.java","public class FraudDetector implements PaymentObserver {","    private static final Logger log = LoggerFactory.getLogger(FraudDetector.class);","    @Override","    public void onPaymentProcessed(String userId, double amount, String status) {","        if (amount > 50000) {","            log.warn(\"FRAUD ALERT userId={} amount={} exceeds threshold\", userId, amount);","        }","    }","}"] },
      { type: "code", lang: "java", lines: ["// File: src/main/java/com/wallet/observer/PaymentEventPublisher.java","public class PaymentEventPublisher {","    private final List<PaymentObserver> observers = new ArrayList<>();","","    public void subscribe(PaymentObserver observer) { observers.add(observer); }","    public void unsubscribe(PaymentObserver observer) { observers.remove(observer); }","","    public void publish(String userId, double amount, String status) {","        observers.forEach(o -> o.onPaymentProcessed(userId, amount, status));","    }","}"] },
      { type: "bold", value: "Failure simulation — no observer pattern, direct coupling:" },
      { type: "code", lang: "java", lines: ["// In PaymentService, call AuditLogger and FraudDetector directly","// Now add a third notifier — you must modify PaymentService","// OCP violation — PaymentService is not closed for modification","// Restore the Observer pattern — adding new observers never touches PaymentService"] },
      { type: "exercise", label: "Build 3 Observer tests — 60 mins" },
      { type: "code", lang: "java", lines: ["class PaymentEventPublisherTest {","    private PaymentEventPublisher publisher;","","    @BeforeEach","    void setUp() { publisher = new PaymentEventPublisher(); }","","    @Test","    void publish_withOneObserver_callsObserver() {","        List<String> received = new ArrayList<>();","        publisher.subscribe((u, a, s) -> received.add(u + \"|\" + s));","        publisher.publish(\"user1\", 500.0, \"SUCCESS\");","        assertEquals(1, received.size());","    }","","    @Test","    void publish_withMultipleObservers_allReceiveEvent() {","        AtomicInteger count = new AtomicInteger(0);","        publisher.subscribe((u, a, s) -> count.incrementAndGet());","        publisher.subscribe((u, a, s) -> count.incrementAndGet());","        publisher.publish(\"user1\", 100.0, \"SUCCESS\");","        assertEquals(2, count.get());","    }","","    @Test","    void publish_afterUnsubscribe_observerNotCalled() {","        AtomicInteger count = new AtomicInteger(0);","        PaymentObserver obs = (u, a, s) -> count.incrementAndGet();","        publisher.subscribe(obs);","        publisher.unsubscribe(obs);","        publisher.publish(\"user1\", 100.0, \"SUCCESS\");","        assertEquals(0, count.get());","    }","}"] },
      { type: "done", value: "Observer pattern implemented. 3 tests green. You explain why Observer satisfies OCP without touching PaymentService." },
    ],
  },
  w1d7: {
    id: "w1d7", weekId: "w1", dayNumber: 7, dayLabel: "Sunday", estimatedMinutes: 150,
    title: "Week 1 Checkpoint",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Answer every question from memory. No notes. No IDE." },
      { type: "bullet", value: "Why can an interface not hold instance state? What exactly happens at compile time if you try?" },
      { type: "bullet", value: "After Java 8, what is the one remaining reason to choose an abstract class over an interface?" },
      { type: "bullet", value: "Explain LSP using your PaymentStrategy implementations as the example" },
      { type: "bullet", value: "Walk through Strategy pattern using PaymentContext — what problem does it solve?" },
      { type: "bullet", value: "Why does JwtTokenProvider need volatile? What breaks without it under concurrent access?" },
      { type: "bullet", value: "Why does Builder exist? Show a PaymentRequest construction with 5 fields — would a constructor be readable?" },
      { type: "bullet", value: "How does Observer satisfy OCP? What would violate OCP in the same scenario?" },
      { type: "bullet", value: "What does git rebase -i do and when would you use it before pushing?" },
      { type: "bullet", value: "What is inside a fat jar and why does Spring Boot package it that way?" },
      { type: "warn", value: "If you cannot answer any question without notes — do not move to Week 2. Spend the remaining time on the weak area. Week 2 builds directly on this foundation." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Run the full test suite first:" },
      { type: "code", lang: "bash", lines: ["mvn clean test","# All tests must be green before attempting the checkpoint"] },
      { type: "bold", value: "Checkpoint questions — answer each one out loud:" },
      { type: "bold", value: "OOP:" },
      { type: "bullet", value: "Why can an interface not hold instance state? What exactly happens if you try?" },
      { type: "bullet", value: "After Java 8, what is the one remaining reason to use an abstract class over an interface?" },
      { type: "bullet", value: "Explain LSP using your PaymentStrategy implementations as the example" },
      { type: "bold", value: "Design patterns:" },
      { type: "bullet", value: "Walk through Strategy pattern using PaymentContext — what problem does it solve?" },
      { type: "bullet", value: "Why does PaymentFactory use a static method? What would be different if it were an instance method?" },
      { type: "bullet", value: "Why does JwtTokenProvider need volatile? What breaks without it?" },
      { type: "bullet", value: "Why does Builder exist? Show a PaymentRequest construction with 5 fields — would a constructor be readable?" },
      { type: "bullet", value: "How does Observer satisfy OCP? What would violate OCP in the same scenario?" },
      { type: "bold", value: "Git and Maven:" },
      { type: "bullet", value: "What does git rebase -i do and when would you use it before pushing?" },
      { type: "bullet", value: "What is the difference between test scope and compile scope in Maven?" },
      { type: "bullet", value: "What is inside a fat jar and why does Spring Boot package it that way?" },
      { type: "bold", value: "GitHub check:" },
      { type: "bullet", value: "Every commit message follows the type: description format" },
      { type: "bullet", value: "No WIP, fix, fix2, or asdf commits in history" },
      { type: "bullet", value: "All 8+ tests are committed" },
      { type: "warn", value: "If you cannot answer any checkpoint question without notes — do not move to Week 2. Spend the remaining time on the weak area. Week 2 builds directly on top of this." },
      { type: "market", value: "Passing Week 1 means you understand OOP at the depth that separates candidates in product company interviews. Most 1 YOE developers cannot explain volatile in Singleton or Observer's relationship to OCP. You now can." },
    ],
  },
};

export default WEEK1_TOPICS;
