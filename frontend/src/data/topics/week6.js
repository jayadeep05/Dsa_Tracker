// Topic content for Week 6 — Redis Caching + Idempotency + Rate Limiting

const WEEK6_TOPICS = {
  w6d36: {
    id: "w6d36", weekId: "w6", dayNumber: 36, dayLabel: "Monday", estimatedMinutes: 90,
    title: "Redis Caching",
    subtitle: "Project: service/AccountService.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Redis fundamentals — what you must understand before using it:" },
      { type: "bullet", value: "What Redis is — an in-memory data structure store. Read latency: microseconds. Database read latency: milliseconds. 100-1000x faster." },
      { type: "bullet", value: "Why Redis is NOT a replacement for a database — data lives in RAM. Process restart = data loss by default. Use for data you can rebuild from the DB." },
      { type: "bullet", value: "What TTL (time-to-live) is — a key expires automatically after N seconds. Prevents stale data from living forever." },
      { type: "empty", value: "" },
      { type: "bold", value: "Cache-aside pattern — what you must understand:" },
      { type: "bullet", value: "How it works — check cache first. If HIT, return cached value. If MISS, load from DB, store in cache, return value." },
      { type: "bullet", value: "Why it is called \"cache-aside\" — the application manages the cache manually. The cache is beside the DB, not in front of it." },
      { type: "bullet", value: "Cache invalidation on write — when you UPDATE the DB, DELETE the corresponding cache key. Force the next read to reload fresh from DB." },
      { type: "bullet", value: "Why DELETE not UPDATE on write — correctly updating a cached value under concurrent writes is complex. Delete is simpler and always correct." },
      { type: "bullet", value: "The cache inconsistency bug — you UPDATE the DB but forget to DELETE the cache. Every subsequent read gets stale data until TTL expires." },
      { type: "bullet", value: "Why TTL alone is not sufficient for balance data — TTL means stale data for up to 60 seconds after every write. Unacceptable for account balances." },
      { type: "tip", value: "The most common Redis production bug: updating DB without invalidating cache. Always delete on write." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "exercise", label: "80 mins — build cache layer" },
      { type: "code", lang: "yaml", lines: ["# application.yml", "spring.data.redis.host: localhost", "spring.data.redis.port: 6379"] },
      { type: "p", value: "File: src/main/java/com/wallet/service/AccountService.java" },
      { type: "code", lang: "java", lines: [
        "@Service", "public class AccountService {",
        "    private static final Logger log = LoggerFactory.getLogger(AccountService.class);",
        "    private static final String BALANCE_KEY = \"wallet:balance:\";",
        "    private static final long TTL_SECONDS = 60;", "",
        "    public BigDecimal getBalance(Long accountId) {",
        "        String key = BALANCE_KEY + accountId;",
        "        Object cached = redisTemplate.opsForValue().get(key);",
        "        if (cached != null) {",
        "            log.debug(\"Cache HIT balance for accountId={}\", accountId);",
        "            return new BigDecimal(cached.toString());",
        "        }",
        "        log.debug(\"Cache MISS balance for accountId={}\", accountId);",
        "        Account account = accountRepo.findById(accountId).orElseThrow();",
        "        redisTemplate.opsForValue().set(",
        "            key, account.getBalance().toString(), TTL_SECONDS, TimeUnit.SECONDS);",
        "        return account.getBalance();",
        "    }", "",
        "    @Transactional",
        "    public void debit(Long accountId, BigDecimal amount) {",
        "        Account account = accountRepo.findByIdWithLock(accountId);",
        "        if (account.getBalance().compareTo(amount) < 0)",
        "            throw new InsufficientBalanceException(",
        "                \"Balance \" + account.getBalance() + \" < \" + amount);",
        "        account.setBalance(account.getBalance().subtract(amount));",
        "        accountRepo.save(account);",
        "        // Invalidate cache on write — always",
        "        redisTemplate.delete(BALANCE_KEY + accountId);",
        "        log.info(\"Debited accountId={} amount={}\", accountId, amount);",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Failure simulation — cache inconsistency:" },
      { type: "p", value: "Remove the redisTemplate.delete() call after debit. Update the balance via a direct DB UPDATE. Call getBalance() — it returns the stale cached value. Restore the delete. Verify cache is always fresh after writes. This is the most common Redis bug in production." },
      { type: "done", value: "Cache hit/miss is logged. Cache is always invalidated on writes. Tests verify both paths." },
    ],
  },
  w6d37: {
    id: "w6d37", weekId: "w6", dayNumber: 37, dayLabel: "Tuesday", estimatedMinutes: 90,
    title: "Idempotency + Rate Limiting",
    subtitle: "Project: service/IdempotencyService.java + RateLimiterService.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Idempotency service — what you must understand:" },
      { type: "bullet", value: "What idempotency means — performing the same operation multiple times produces the same result as performing it once" },
      { type: "bullet", value: "Why payment APIs must be idempotent — network timeouts cause clients to retry. Without idempotency, one timeout = duplicate payment." },
      { type: "bullet", value: "How Redis-backed idempotency works — store idempotency_key → serialized_response for 24 hours. On duplicate request, return the stored response immediately." },
      { type: "bullet", value: "Why 24 hours TTL — covers realistic retry windows. After 24 hours, the key can safely be reused for a new payment." },
      { type: "empty", value: "" },
      { type: "bold", value: "Rate limiting with Lua scripts — what you must understand:" },
      { type: "bullet", value: "What rate limiting is — enforcing a maximum number of requests per user per time window" },
      { type: "bullet", value: "The race condition without Lua — INCR key and EXPIRE key are two separate Redis commands. Between them, another thread can call INCR and the EXPIRE never gets set. The key never resets." },
      { type: "bullet", value: "What a Lua script does in Redis — executes multiple Redis commands as one atomic operation in a single server-side transaction" },
      { type: "bullet", value: "Why \"atomic\" matters — INCR and EXPIRE are guaranteed to execute together with nothing in between. The race condition is eliminated." },
      { type: "bullet", value: "The sliding window counter algorithm — INCR on each request, EXPIRE only on first request (count == 1), compare count against MAX limit" },
      { type: "tip", value: "The race condition between INCR and EXPIRE without Lua is a real production bug. This is exactly the problem Lua scripting in Redis solves." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Idempotency service (40 mins):" },
      { type: "code", lang: "java", lines: [
        "@Service", "public class IdempotencyService {",
        "    private static final String KEY = \"wallet:idempotency:\";",
        "    private static final long TTL = 86400; // 24 hours", "",
        "    public Optional<String> getResult(String idempotencyKey) {",
        "        Object result = redisTemplate.opsForValue().get(KEY + idempotencyKey);",
        "        return Optional.ofNullable(result).map(Object::toString);",
        "    }", "",
        "    public void storeResult(String idempotencyKey, String result) {",
        "        redisTemplate.opsForValue().set(",
        "            KEY + idempotencyKey, result, TTL, TimeUnit.SECONDS);",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Rate limiter with Lua (50 mins):" },
      { type: "code", lang: "java", lines: [
        "@Service", "public class RateLimiterService {",
        "    private static final int MAX = 10;",
        "    private static final int WINDOW = 60;",
        "    // Lua script: INCR + EXPIRE in one atomic operation",
        "    private static final String SCRIPT =",
        "        \"local c = redis.call('INCR', KEYS[1]) \" +",
        "        \"if c == 1 then redis.call('EXPIRE', KEYS[1], ARGV[1]) end \" +",
        "        \"return c\";", "",
        "    public boolean isAllowed(String userId) {",
        "        Long count = redisTemplate.execute(",
        "            new DefaultRedisScript<>(SCRIPT, Long.class),",
        "            List.of(\"wallet:rate:\" + userId),",
        "            String.valueOf(WINDOW));",
        "        return count != null && count <= MAX;",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Failure simulation — race condition without Lua:" },
      { type: "p", value: "Replace the Lua script with two separate calls: redisTemplate.opsForValue().increment(key) and redisTemplate.expire(key, ...). Launch 50 threads simultaneously. Observe that some keys never get an expiry set — rate limit never resets. Restore Lua script. This is exactly why Lua exists." },
      { type: "done", value: "Idempotency and rate limiting work. You explain why Lua makes both atomic." },
    ],
  },
  w6d38: {
    id: "w6d38", weekId: "w6", dayNumber: 38, dayLabel: "Wednesday", estimatedMinutes: 90,
    title: "API Consumption — WebClient + Retry + Timeout",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Why outbound HTTP calls need resilience patterns:" },
      { type: "bullet", value: "External services fail — fraud check API, bank verification API, SMS gateway: all can go down" },
      { type: "bullet", value: "External services become slow — a 10-second response from the fraud API ties up your thread for 10 seconds" },
      { type: "bullet", value: "Without timeouts — one slow external call can hold a thread indefinitely. Thread pool exhaustion follows. Your API becomes unresponsive." },
      { type: "empty", value: "" },
      { type: "bold", value: "WebClient — what you must understand:" },
      { type: "bullet", value: "What WebClient is — Spring\'s non-blocking reactive HTTP client. Replaces RestTemplate for new Spring Boot code." },
      { type: "bullet", value: "responseTimeout — maximum time to wait for a complete response from the server. After this, throw WebClientRequestException." },
      { type: "bullet", value: "CONNECT_TIMEOUT — maximum time to wait for the TCP connection to be established. Separate from and shorter than response timeout." },
      { type: "empty", value: "" },
      { type: "bold", value: "Retry with exponential backoff — what you must understand:" },
      { type: "bullet", value: "What exponential backoff is — first retry after 200ms, second after 400ms, third after 800ms. Gives a struggling service time to recover." },
      { type: "bullet", value: "What to retry — network errors (WebClientRequestException), 503 Service Unavailable, 429 Too Many Requests with Retry-After header" },
      { type: "bullet", value: "What NOT to retry — 400 Bad Request (your request is wrong), 422 Unprocessable (business logic rejected it)" },
      { type: "bullet", value: "What onErrorReturn does — after all retries are exhausted, return a safe fallback value instead of propagating the exception to the caller" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "30 mins — outbound calls are as important as inbound" },
      { type: "p", value: "Your payment service will call external APIs: fraud detection, bank verification, SMS gateway. These external services fail, timeout, and return errors. You must handle all of these." },
      { type: "exercise", label: "Build WebClient with resilience — 60 mins" },
      { type: "code", lang: "java", lines: [
        "@Configuration", "public class WebClientConfig {",
        "    @Bean", "    public WebClient fraudCheckClient() {",
        "        return WebClient.builder()",
        "            .baseUrl(\"https://fraud-api.example.com\")",
        "            .defaultHeader(\"Content-Type\", \"application/json\")",
        "            .clientConnector(new ReactorClientHttpConnector(",
        "                HttpClient.create()",
        "                    .responseTimeout(Duration.ofSeconds(2))",
        "                    .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 1000)",
        "            ))",
        "            .build();",
        "    }",
        "}", "",
        "@Service", "public class FraudCheckService {",
        "    private static final Logger log = LoggerFactory.getLogger(FraudCheckService.class);", "",
        "    public String checkFraud(String userId, BigDecimal amount) {",
        "        return fraudCheckClient.post()",
        "            .uri(\"/check\")",
        "            .bodyValue(Map.of(\"userId\", userId, \"amount\", amount))",
        "            .retrieve()",
        "            .bodyToMono(String.class)",
        "            .retryWhen(Retry.backoff(3, Duration.ofMillis(200))",
        "                .filter(ex -> ex instanceof WebClientRequestException))",
        "            .timeout(Duration.ofSeconds(5))",
        "            .onErrorReturn(\"UNKNOWN\")",
        "            .block();",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Failure simulation — simulate timeout:" },
      { type: "code", lang: "bash", lines: [
        "# Use httpbin to simulate a slow response",
        "curl https://httpbin.org/delay/10  # delays 10 seconds",
        "# Point your WebClient at this URL",
        "# Watch the 2s responseTimeout kick in",
        "# Watch retry logic fire 3 times",
        "# Watch onErrorReturn return 'UNKNOWN'"
      ]},
      { type: "done", value: "Outbound calls have timeouts and retry. Downstream failures return a safe fallback, not a 500 error." },
    ],
  },
  w6d39: {
    id: "w6d39", weekId: "w6", dayNumber: 39, dayLabel: "Thursday", estimatedMinutes: 90,
    title: "Bean Validation + API Versioning + Production Mindset",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Bean Validation — what you must understand:" },
      { type: "bullet", value: "What Bean Validation is — JSR-380 standard annotations on DTO fields. Spring validates automatically when @Valid is used on @RequestBody." },
      { type: "bullet", value: "Common annotations — @NotNull (not null), @NotBlank (not null and not whitespace), @Positive (number > 0), @DecimalMax (upper bound), @Pattern (regex)" },
      { type: "bullet", value: "What @Valid on @RequestBody does — triggers validation of the entire DTO before the controller method is entered" },
      { type: "bullet", value: "What happens on validation failure — MethodArgumentNotValidException is thrown, caught by GlobalExceptionHandler, returns 400 with field-level error messages" },
      { type: "empty", value: "" },
      { type: "bold", value: "API versioning — what you must understand:" },
      { type: "bullet", value: "Why versioning is necessary — you will make breaking changes. Without versioning, existing clients break when you deploy." },
      { type: "bullet", value: "URL versioning (/api/v1/, /api/v2/) — most common approach. Visible, easy to route, easy to document." },
      { type: "bullet", value: "The non-negotiable rule — never modify v1 in place. Add v2 alongside it. Clients on v1 keep working." },
      { type: "empty", value: "" },
      { type: "bold", value: "Graceful shutdown — what you must understand:" },
      { type: "bullet", value: "What graceful shutdown means — stop accepting new requests, but allow in-flight requests to complete before the process exits" },
      { type: "bullet", value: "Why it is critical for payments — without it, a rolling deployment kills active payment transactions mid-flight. Money in unknown state." },
      { type: "bullet", value: "What SIGTERM is — the signal Kubernetes and Docker send to a container when stopping it. JVM receives it and begins graceful shutdown." },
      { type: "bullet", value: "What server.shutdown=graceful does — Spring Boot stops accepting new requests immediately on SIGTERM but waits up to the configured timeout for in-flight requests to finish" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Bean Validation on DTOs (30 mins):" },
      { type: "code", lang: "java", lines: [
        "public class PaymentRequest {",
        "    @NotBlank(message = \"paymentType is required\")",
        "    @Pattern(regexp = \"UPI|CARD|NETBANKING\", message = \"invalid payment type\")",
        "    private String paymentType;", "",
        "    @NotNull @Positive(message = \"amount must be positive\")",
        "    @DecimalMax(value = \"1000000\", message = \"amount exceeds maximum\")",
        "    private BigDecimal amount;", "",
        "    @NotBlank private String userId;",
        "}"
      ]},
      { type: "bold", value: "API versioning (20 mins):" },
      { type: "p", value: "Your controller is @RequestMapping(\"/api/v1/payments\"). v1 is in the URL. When breaking changes are needed, create v2 alongside v1 — never change v1 in place." },
      { type: "bold", value: "Production mindset — graceful shutdown (40 mins):" },
      { type: "code", lang: "yaml", lines: ["# application.yml", "server.shutdown: graceful", "spring.lifecycle.timeout-per-shutdown-phase: 30s"] },
      { type: "p", value: "Graceful shutdown means: stop accepting new requests, but finish processing in-flight requests before shutting down. Without this, a deployment kills active payment transactions mid-flight." },
      { type: "bold", value: "Failure simulation:" },
      { type: "code", lang: "bash", lines: [
        "# Start a slow 10-second payment request",
        "# While it's running: kill -15 <app-pid>  (SIGTERM — graceful)",
        "# With graceful shutdown: request completes before process exits",
        "# Without it: request gets killed halfway — payment in unknown state"
      ]},
      { type: "done", value: "Validation returns correct 400 errors. Graceful shutdown is configured and tested." },
    ],
  },
  w6d40: {
    id: "w6d40", weekId: "w6", dayNumber: 40, dayLabel: "Friday", estimatedMinutes: 90,
    title: "System Design — URL Shortener + Notification System",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "URL Shortener — topics to understand before designing:" },
      { type: "bullet", value: "How Base62 encoding converts an auto-increment integer to a short alphanumeric code — why 7 chars covers trillions of URLs" },
      { type: "bullet", value: "Why 302 redirect is used instead of 301 — 302 is temporary, browser does not cache it. Every redirect hits your server = analytics. 301 is permanent, browser caches it = untrackable." },
      { type: "bullet", value: "Role of CDN — popular short URLs are cached at edge nodes globally. The redirect never reaches your origin servers." },
      { type: "bullet", value: "Why read replicas help — URL shorteners are overwhelmingly read-heavy (100:1 read:write). Distribute reads across replicas." },
      { type: "empty", value: "" },
      { type: "bold", value: "Notification System — topics to understand before designing:" },
      { type: "bullet", value: "Why Kafka decouples the notification sender from delivery channels — the sender fires and forgets. Kafka absorbs traffic spikes. Workers deliver at their own rate." },
      { type: "bullet", value: "What a Dead Letter Queue (DLQ) is — failed messages after all retries are moved here for manual inspection. Nothing is silently dropped." },
      { type: "bullet", value: "Why different priority Kafka topics exist — OTP must deliver in under 1 second. Marketing batch can wait minutes." },
      { type: "bullet", value: "What each channel requires — email (SendGrid API), SMS (Twilio API), push notifications (FCM for Android, APNs for iOS)" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "URL Shortener (45 mins — notebook):" },
      { type: "p", value: "Scale: 100M URLs/day written, 10B redirects/day read. Read:write = 100:1." },
      { type: "p", value: "Short code: Base62(auto-increment ID). 7 chars = 3.5 trillion unique codes." },
      { type: "p", value: "Architecture: Client → CDN (caches popular URLs) → LB → App → MySQL (write) + Redis (read, 99% hit rate)." },
      { type: "p", value: "Key decision: 302 not 301. 302 hits server every time = trackable analytics. 301 browser caches = untrackable." },
      { type: "p", value: "Bottleneck at 10x: single MySQL write node. Fix: read replicas, then sharding by URL hash." },
      { type: "bold", value: "Notification System (35 mins — notebook):" },
      { type: "p", value: "10M notifications/day. Channels: email, SMS, push. OTP = immediate, marketing = delayed." },
      { type: "p", value: "Architecture: App → Notification Service → Kafka (HIGH/LOW priority topics) → Channel Workers → SendGrid/Twilio/FCM." },
      { type: "p", value: "Why Kafka: decouples sender from delivery. Traffic spike absorption. Retry with DLQ. Different priority lanes." },
      { type: "done", value: "You deliver both designs under 45 minutes each using the 7-step framework." },
    ],
  },
  w6d41: {
    id: "w6d41", weekId: "w6", dayNumber: 41, dayLabel: "Saturday", estimatedMinutes: 150,
    title: "Week 6 Integration — Wire Redis into Full Payment Flow",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "What to understand before integration day:" },
      { type: "bullet", value: "The full payment processing order — idempotency check → rate limit check → async fraud check (starts immediately) → balance debit → wait for fraud result → process payment → store response → cache update → publish event" },
      { type: "bullet", value: "Why fraud check starts asynchronously before debit — fraud check and debit are independent. Starting fraud check early saves latency." },
      { type: "bullet", value: "Why idempotency is checked first — if it is a duplicate, skip all processing and return the cached response. No DB writes, no fraud check, no debit." },
      { type: "bullet", value: "What happens when fraud returns FLAGGED after the debit has already happened — debit must be reversed (credit the amount back). Order of operations determines complexity of rollback." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Full payment flow with all components:" },
      { type: "code", lang: "java", lines: [
        "@Service", "public class PaymentService {",
        "    @Transactional",
        "    public PaymentResponse process(String idempotencyKey, PaymentRequest req) {",
        "        // 1. Check idempotency",
        "        Optional<String> existing = idempotencyService.getResult(idempotencyKey);",
        "        if (existing.isPresent()) return deserialize(existing.get());", "",
        "        // 2. Check rate limit",
        "        if (!rateLimiter.isAllowed(req.getUserId()))",
        "            throw new RateLimitExceededException(\"Too many requests\");", "",
        "        // 3. Fraud check (async, parallel with balance check)",
        "        CompletableFuture<String> fraudFuture =",
        "            CompletableFuture.supplyAsync(() -> fraudService.checkFraud(",
        "                req.getUserId(), req.getAmount()));", "",
        "        // 4. Debit account",
        "        accountService.debit(req.getFromAccountId(), req.getAmount());", "",
        "        // 5. Wait for fraud result",
        "        String fraudResult = fraudFuture.join();",
        "        if (\"FLAGGED\".equals(fraudResult)) {",
        "            accountService.credit(req.getFromAccountId(), req.getAmount());",
        "            throw new FraudDetectedException(\"Payment flagged\");",
        "        }", "",
        "        // 6. Save payment",
        "        Payment payment = paymentRepo.save(buildPayment(req, idempotencyKey));",
        "        PaymentResponse response = buildResponse(payment);", "",
        "        // 7. Store idempotency result",
        "        idempotencyService.storeResult(idempotencyKey, serialize(response));",
        "        return response;",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Hour 2 — Failure simulations for the full flow:" },
      { type: "bullet", value: "Send same idempotency key twice — verify second returns 200 not 201" },
      { type: "bullet", value: "Send 11 requests in 60 seconds — verify 11th returns 429" },
      { type: "bullet", value: "Set account balance to 0 and try debit — verify 422 with correct error" },
      { type: "bullet", value: "Stop Redis with docker-compose stop redis — verify app handles gracefully" },
      { type: "bold", value: "Hour 3 — Debugging with logs:" },
      { type: "p", value: "Make a failed payment. Open logs. Trace the requestId through every log line from filter to service to repository. This is production debugging." },
      { type: "done", value: "Full payment flow works. All failure scenarios tested. You trace a complete request through logs." },
    ],
  },
  w6d42: {
    id: "w6d42", weekId: "w6", dayNumber: 42, dayLabel: "Sunday", estimatedMinutes: 150,
    title: "Week 6 Revision",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Why Testcontainers — what you must understand:" },
      { type: "bullet", value: "What Testcontainers is — starts real Docker containers (MySQL, Redis) automatically during your test run and tears them down after" },
      { type: "bullet", value: "Why unit tests with mocks are not sufficient — mocks do not catch real SQL syntax errors, missing indexes, constraint violations, or actual Redis behaviour" },
      { type: "bullet", value: "What integration tests catch that unit tests miss — real query performance problems, actual foreign key violations, real transaction isolation issues" },
      { type: "bullet", value: "Why H2 in-memory DB is not sufficient — H2 SQL dialect differs from MySQL. Queries that pass in H2 fail in production MySQL." },
      { type: "empty", value: "" },
      { type: "bold", value: "How Testcontainers works — what you must understand:" },
      { type: "bullet", value: "@Testcontainers annotation — activates the Testcontainers lifecycle: start containers before tests, stop after" },
      { type: "bullet", value: "@Container annotation — declares a specific container (MySQLContainer, GenericContainer for Redis)" },
      { type: "bullet", value: "MySQLContainer — starts real MySQL 8.0 with a randomly assigned host port" },
      { type: "bullet", value: "@DynamicPropertySource — injects the container\'s dynamic connection URL, port, and credentials into Spring\'s application context at runtime" },
      { type: "bullet", value: "Why @DynamicPropertySource is required — the container port is randomly assigned. You cannot hardcode it in application.properties." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Answer from memory:" },
      { type: "bullet", value: "Why is Redis cache invalidated on write, not on read?" },
      { type: "bullet", value: "Why does the Lua script make rate limiting atomic?" },
      { type: "bullet", value: "What is idempotency and why does it matter for payment APIs?" },
      { type: "bullet", value: "What is the difference between retry and idempotency?" },
      { type: "bold", value: "Hour 2 — Interview questions:" },
      { type: "bullet", value: "Design a caching strategy for user balance — when to cache, when to invalidate" },
      { type: "bullet", value: "A duplicate payment request arrives 2 minutes after the first. Walk me through what happens." },
      { type: "bullet", value: "Your fraud check service is down. What does your payment flow do?" },
      { type: "warn", value: "If any answer is weak, do not move to Week 7. Fix it first." },
    ],
  },
};

export default WEEK6_TOPICS;
