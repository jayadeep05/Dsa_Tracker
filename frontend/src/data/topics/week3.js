// Topic content for Weeks 3-4
// Each topic is keyed by its ID and contains an array of typed content blocks.
// Types: study, p, bold, bullet, code, exercise, failureSim, done, tip, warn, market

const WEEK3_TOPICS = {
  w3d15: {
    id: "w3d15", weekId: "w3", dayNumber: 15, dayLabel: "Monday", estimatedMinutes: 90,
    title: "Thread Lifecycle + ExecutorService",
    subtitle: "Project: service/PaymentBatchProcessor.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Thread lifecycle — what you must understand:" },
      { type: "bullet", value: "All 6 states and what each means: NEW → RUNNABLE → RUNNING → BLOCKED / WAITING / TIMED_WAITING → TERMINATED" },
      { type: "bullet", value: "BLOCKED — thread is waiting to acquire a synchronized lock held by another thread" },
      { type: "bullet", value: "WAITING — thread called wait() or join() with no timeout. Waiting indefinitely for a signal." },
      { type: "bullet", value: "TIMED_WAITING — thread called sleep(n) or wait(n). Will wake after the timeout." },
      { type: "bullet", value: "Why a thread can be alive but not executing — it is in BLOCKED or WAITING state. This matters when reading thread dumps." },
      { type: "empty", value: "" },
      { type: "bold", value: "Why raw threads are never created directly in production:" },
      { type: "bullet", value: "Memory cost — each thread needs ~1MB stack by default. 10,000 threads = ~10GB RAM for stacks alone." },
      { type: "bullet", value: "OS scheduling overhead — creating and destroying OS threads is an expensive system call" },
      { type: "bullet", value: "No resource control — unbounded thread creation can exhaust memory and crash the JVM" },
      { type: "empty", value: "" },
      { type: "bold", value: "ExecutorService — what you must understand:" },
      { type: "bullet", value: "What ExecutorService is — manages a reusable pool of threads. Tasks are submitted, threads are reused." },
      { type: "bullet", value: "newFixedThreadPool(n) — exactly n threads, always. Predictable resource usage. Prefer over cached pool." },
      { type: "bullet", value: "What Future is — a handle to an async computation. future.get() blocks the calling thread until the result is ready." },
      { type: "bullet", value: "Why future.get() without a timeout is dangerous in production — if the task hangs, the calling thread blocks forever" },
      { type: "bullet", value: "Why pool.shutdown() is always required — daemon vs non-daemon threads. Pool threads are non-daemon. JVM will not exit until they are stopped." },
      { type: "bullet", value: "What awaitTermination() does — waits for already-submitted tasks to finish before returning" },
      { type: "tip", value: "Production rule: always use future.get(timeout, unit). Never call get() without a timeout." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "30 mins" },
      { type: "p", value: "Thread lifecycle — memorise these states: NEW → RUNNABLE → RUNNING → BLOCKED / WAITING / TIMED_WAITING → TERMINATED. An interviewer asking about this wants to know if you understand that a thread can be alive but not running." },
      { type: "p", value: "Raw threads are never used in production. 10,000 threads ≈ 10GB RAM for stacks alone. ExecutorService manages a reusable thread pool:" },
      { type: "code", lang: "java", lines: [
        "// FixedThreadPool: bounded, predictable resource usage",
        "ExecutorService pool = Executors.newFixedThreadPool(10);",
        "",
        "// Submit Callable — gets a Future back",
        "Future<String> future = pool.submit(() -> {",
        "    Thread.sleep(500);",
        "    return \"TXN-\" + UUID.randomUUID();",
        "});",
        "",
        "// Always use timeout — never block forever in production",
        "String txnId = future.get(2, TimeUnit.SECONDS);",
        "",
        "// ALWAYS shutdown — otherwise JVM won't exit",
        "pool.shutdown();",
        "pool.awaitTermination(5, TimeUnit.SECONDS);"
      ]},
      { type: "exercise", label: "40 mins — build and break" },
      { type: "bold", value: "Failure simulation — forgetting shutdown:" },
      { type: "code", lang: "java", lines: [
        "// Build this and run it",
        "ExecutorService pool = Executors.newFixedThreadPool(5);",
        "pool.submit(() -> System.out.println(\"task done\"));",
        "// No shutdown — JVM hangs. Ctrl+C to kill.",
        "// Now add shutdown() and awaitTermination(). JVM exits cleanly."
      ]},
      { type: "bold", value: "Failure simulation — Future.get() without timeout:" },
      { type: "code", lang: "java", lines: [
        "Future<String> future = pool.submit(() -> {",
        "    Thread.sleep(Integer.MAX_VALUE); // sleeps forever",
        "    return \"done\";",
        "});",
        "future.get(); // hangs forever — never do this in production",
        "// Fix: future.get(2, TimeUnit.SECONDS) throws TimeoutException"
      ]},
      { type: "done", value: "You create ExecutorService, submit tasks, get results with timeout, and shut down cleanly." },
    ],
  },
  w3d16: {
    id: "w3d16", weekId: "w3", dayNumber: 16, dayLabel: "Tuesday", estimatedMinutes: 90,
    title: "synchronized + volatile + Java Memory Model",
    subtitle: "Project: service/BalanceManager.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Java Memory Model — the root cause of all race conditions:" },
      { type: "bullet", value: "What the Java Memory Model defines — each CPU core has its own cache. Writes by one thread are not immediately visible to other threads." },
      { type: "bullet", value: "What a race condition is — two threads reading and writing the same data without coordination, producing incorrect results" },
      { type: "bullet", value: "The lost update problem — Thread 1 reads balance=1000, Thread 2 reads balance=1000, both add 500, both write 1500. The correct result is 2000. Money disappeared." },
      { type: "empty", value: "" },
      { type: "bold", value: "synchronized — what it provides:" },
      { type: "bullet", value: "Mutual exclusion — only one thread can execute the synchronized block at a time" },
      { type: "bullet", value: "Memory visibility — changes made inside the block are flushed to main memory and visible to all threads" },
      { type: "bullet", value: "The cost — threads queue up waiting for the lock. Heavy use of synchronized creates a bottleneck." },
      { type: "empty", value: "" },
      { type: "bold", value: "volatile — what it provides and does NOT provide:" },
      { type: "bullet", value: "What volatile provides — memory visibility. Reads and writes go directly to main memory, bypassing CPU cache." },
      { type: "bullet", value: "What volatile does NOT provide — atomicity. count++ is 3 separate operations (read, increment, write). volatile does not make this atomic." },
      { type: "bullet", value: "When volatile is the correct tool — boolean flags written by one thread and read by others: volatile boolean isRunning = true" },
      { type: "bullet", value: "When volatile is the wrong tool — any read-modify-write operation. Two threads reading volatile count=0, both incrementing, both writing 1. Final count: 1, not 2." },
      { type: "empty", value: "" },
      { type: "bold", value: "AtomicInteger / AtomicLong and CAS — what you must understand:" },
      { type: "bullet", value: "What CAS means — Compare And Swap. A single hardware-level atomic CPU instruction." },
      { type: "bullet", value: "How CAS works — \"if the current value is X, atomically set it to Y and return true. Otherwise return false.\" All in one instruction." },
      { type: "bullet", value: "Why CAS is faster than synchronized — no lock, no thread queuing, no OS-level context switch" },
      { type: "bullet", value: "The CAS loop pattern — read current value, compute new value, attempt CAS. If CAS fails (someone else changed it), loop and retry." },
      { type: "bullet", value: "What compareAndSet() returns — true if the swap succeeded, false if someone else changed the value between read and write" },
      { type: "tip", value: "The BalanceManager debit() method uses a CAS loop. This is exactly how real payment systems handle concurrent balance updates without locks." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "50 mins" },
      { type: "p", value: "Java Memory Model: each thread has its own CPU cache copy of variables. Without synchronization, Thread 1 reads balance=1000, Thread 2 reads balance=1000, both add 500, both write 1500. Final balance: 1500 instead of 2000. Money disappeared. This is a race condition." },
      { type: "code", lang: "java", lines: [
        "// synchronized: mutual exclusion + memory visibility",
        "public class BankAccount {",
        "    private double balance;",
        "    public synchronized void deposit(double amount)  { balance += amount; }",
        "    public synchronized double getBalance()           { return balance; }",
        "}",
        "",
        "// volatile: visibility only — NOT atomicity",
        "private volatile boolean isRunning = true; // correct use",
        "",
        "// volatile NOT enough for this:",
        "private volatile int count = 0;",
        "count++; // read + increment + write = 3 separate operations",
        "// Two threads can both read 0, both write 1 — final count: 1 not 2",
        "",
        "// AtomicInteger: CPU-level CAS — correct",
        "private AtomicInteger count = new AtomicInteger(0);",
        "count.incrementAndGet(); // atomic at hardware level"
      ]},
      { type: "exercise", label: "Build BalanceManager — 40 mins" },
      { type: "p", value: "File: src/main/java/com/wallet/service/BalanceManager.java" },
      { type: "code", lang: "java", lines: [
        "public class BalanceManager {",
        "    private static final Logger log = LoggerFactory.getLogger(BalanceManager.class);",
        "    private final ConcurrentHashMap<String, AtomicLong> balances =",
        "        new ConcurrentHashMap<>();",
        "",
        "    public void createAccount(String userId, long initialBalance) {",
        "        balances.put(userId, new AtomicLong(initialBalance));",
        "        log.info(\"Account created userId={} balance={}\", userId, initialBalance);",
        "    }",
        "",
        "    public boolean debit(String userId, long amount) {",
        "        AtomicLong balance = balances.get(userId);",
        "        if (balance == null) throw new IllegalArgumentException(\"Unknown user: \" + userId);",
        "        long current;",
        "        do {",
        "            current = balance.get();",
        "            if (current < amount) {",
        "                log.warn(\"Insufficient balance userId={} balance={} requested={}\",",
        "                    userId, current, amount);",
        "                return false;",
        "            }",
        "        } while (!balance.compareAndSet(current, current - amount));",
        "        log.info(\"Debit successful userId={} amount={} newBalance={}\",",
        "            userId, amount, balance.get());",
        "        return true;",
        "    }",
        "",
        "    public void credit(String userId, long amount) {",
        "        balances.computeIfAbsent(userId, k -> new AtomicLong(0)).addAndGet(amount);",
        "    }",
        "",
        "    public long getBalance(String userId) {",
        "        AtomicLong b = balances.get(userId);",
        "        return b == null ? 0 : b.get();",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Failure simulation:" },
      { type: "code", lang: "java", lines: [
        "// Replace AtomicLong debit with non-atomic version",
        "long current = balance.get();",
        "balance.set(current - amount); // race condition here",
        "// Run 100 threads debiting 10 from balance of 10000",
        "// Final balance will NOT be 9000 — race condition causes wrong result",
        "// Restore CAS loop. Verify final balance is always correct."
      ]},
      { type: "done", value: "BalanceManager is thread-safe. You demonstrate the race condition and explain why CAS fixes it." },
    ],
  },
  w3d17: {
    id: "w3d17", weekId: "w3", dayNumber: 17, dayLabel: "Wednesday", estimatedMinutes: 90,
    title: "CompletableFuture",
    subtitle: "Project: service/PaymentBatchProcessor.java",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Why CompletableFuture exists — the problem with Future:" },
      { type: "bullet", value: "Future.get() blocks the calling thread. You cannot chain operations. You cannot handle failures cleanly. You cannot compose multiple futures." },
      { type: "bullet", value: "CompletableFuture adds — non-blocking chaining, parallel composition, and declarative exception handling" },
      { type: "empty", value: "" },
      { type: "bold", value: "Core operations — what each does and when to use it:" },
      { type: "bullet", value: "supplyAsync(() -> result) — runs the lambda asynchronously on a thread pool, returns a CompletableFuture<T>" },
      { type: "bullet", value: "thenApply(fn) — transforms the result with a synchronous function. Like map(). Use when the transformation itself is not async." },
      { type: "bullet", value: "thenCompose(fn) — chains a dependent async step. Like flatMap(). Use when Step 2 is also async AND depends on Step 1\'s result." },
      { type: "bullet", value: "thenCombine(other, fn) — runs two independent futures simultaneously and combines their results when both complete" },
      { type: "bullet", value: "exceptionally(fn) — catches any exception in the chain and returns a fallback value. Prevents the exception from propagating." },
      { type: "bullet", value: "join() — like get() but throws unchecked exception. Use inside lambdas where checked exceptions are not allowed." },
      { type: "empty", value: "" },
      { type: "bold", value: "The key distinction you must internalize:" },
      { type: "bullet", value: "thenCompose vs thenCombine — use compose when Step 2 depends on Step 1\'s result. Use combine when both steps are fully independent and can run in parallel." },
      { type: "bullet", value: "Wallet example — fraud check and bank validation do not depend on each other. Run both with thenCombine. Sequential wastes the time of the faster check." },
      { type: "tip", value: "If fraudCheck=500ms and bankValidation=700ms: sequential = 1200ms. thenCombine = ~700ms. This is the real performance difference." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "80 mins — this is how modern Java does async" },
      { type: "p", value: "CompletableFuture runs tasks asynchronously and lets you chain, combine, and handle failures without blocking threads. In your wallet API, fraud check and bank validation run in parallel — not sequentially." },
      { type: "code", lang: "java", lines: [
        "// thenApply — transform result synchronously",
        "CompletableFuture.supplyAsync(() -> 500.0)",
        "    .thenApply(amount -> amount * 0.02) // fee calculation",
        "    .thenApply(fee -> \"Fee: \" + fee);",
        "",
        "// thenCompose — chain dependent futures (flatMap)",
        "// Use when Step 2 depends on Step 1's result",
        "CompletableFuture<String> chain =",
        "    fraudCheck(\"PAY001\")",
        "        .thenCompose(result -> {",
        "            if (result.equals(\"CLEAN\")) return processPayment(\"PAY001\", 500.0);",
        "            return CompletableFuture.completedFuture(\"REJECTED\");",
        "        });",
        "",
        "// thenCombine — parallel independent futures",
        "// Use when Step A and Step B are independent — run simultaneously",
        "long start = System.currentTimeMillis();",
        "CompletableFuture<String> result =",
        "    fraudCheck(\"PAY002\")              // starts immediately",
        "        .thenCombine(",
        "            bankValidation(\"ACC001\"),  // also starts immediately",
        "            (fraud, bank) -> fraud + \"|\" + bank",
        "        );",
        "// If fraudCheck=500ms, bankValidation=700ms",
        "// Sequential: 1200ms. Parallel: ~700ms. This is the win.",
        "",
        "// exceptionally — fallback on any failure",
        "fraudCheck(\"PAY003\")",
        "    .thenCompose(r -> processPayment(\"PAY003\", 100.0))",
        "    .exceptionally(ex -> {",
        "        log.error(\"Payment failed: {}\", ex.getMessage());",
        "        return \"FAILED\";",
        "    });"
      ]},
      { type: "bold", value: "Failure simulation:" },
      { type: "code", lang: "java", lines: [
        "// Replace thenCombine with sequential calls",
        "String fraud = fraudCheck(\"P\").get();    // blocks until done",
        "String bank  = bankValidation(\"A\").get(); // then this runs",
        "// Measure time. Then switch to thenCombine. Measure again.",
        "// You will see the difference in milliseconds."
      ]},
      { type: "done", value: "You use thenCompose, thenCombine, and exceptionally. You measure and prove thenCombine is faster than sequential." },
    ],
  },
  w3d18: {
    id: "w3d18", weekId: "w3", dayNumber: 18, dayLabel: "Thursday", estimatedMinutes: 90,
    title: "Deadlocks + Docker Basics",
    subtitle: "Project: Dockerfile + docker-compose.yml",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Deadlocks — what you must understand:" },
      { type: "bullet", value: "What a deadlock is — Thread 1 holds lock A and waits for B. Thread 2 holds B and waits for A. Both wait forever. Neither can proceed." },
      { type: "bullet", value: "The four conditions that together cause deadlock — mutual exclusion, hold and wait, no preemption, circular wait" },
      { type: "bullet", value: "How to prevent deadlock — always acquire locks in the same globally agreed order (e.g. lowest account ID first)" },
      { type: "bullet", value: "Why lock ordering works — if all threads agree to acquire locks in the same order, circular wait becomes impossible" },
      { type: "bullet", value: "What a thread dump is — a snapshot of every thread\'s state, what it holds, and what it is waiting for" },
      { type: "bullet", value: "How to read a thread dump — find BLOCKED threads, find what each is waiting on, trace the circular chain" },
      { type: "bullet", value: "How to take a thread dump — kill -3 <pid> on Linux, or Run > Dump Threads in IntelliJ" },
      { type: "tip", value: "Reading a thread dump is a production skill you will use when a service hangs at 2am and has no debugger attached." },
      { type: "empty", value: "" },
      { type: "bold", value: "Docker — what you must understand before writing the Dockerfile:" },
      { type: "bullet", value: "What problem Docker solves — \"it works on my machine\". Docker packages app + runtime + dependencies into a container image." },
      { type: "bullet", value: "What an image is — a read-only, layered template. What a container is — a running instance of an image." },
      { type: "bullet", value: "What a Dockerfile is — sequential instructions to build an image layer by layer. Each instruction is a layer." },
      { type: "bullet", value: "What docker-compose is — defines and runs multiple containers together. Your app + MySQL + Redis in one file." },
      { type: "bullet", value: "Why volumes exist in docker-compose — MySQL data persists across container restarts without volumes" },
      { type: "bullet", value: "Essential commands to know — docker ps, docker logs, docker exec -it, docker-compose up -d, docker-compose stop" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Deadlocks (30 mins):" },
      { type: "p", value: "Classic deadlock: Thread 1 holds lock A, waits for B. Thread 2 holds B, waits for A. Both wait forever. For a wallet transfer: always acquire locks in the same order — lowest account ID first." },
      { type: "code", lang: "java", lines: [
        "public void transfer(Account from, Account to, double amount) {",
        "    // Always lock lower ID first — prevents deadlock",
        "    Account first  = from.getId() < to.getId() ? from : to;",
        "    Account second = from.getId() < to.getId() ? to   : from;",
        "    synchronized (first) {",
        "        synchronized (second) {",
        "            from.debit(amount);",
        "            to.credit(amount);",
        "        }",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Failure simulation — create a real deadlock:" },
      { type: "code", lang: "java", lines: [
        "// Thread 1: locks account A, then tries to lock B",
        "// Thread 2: locks account B, then tries to lock A",
        "// Run both simultaneously — JVM hangs",
        "// Take a thread dump: kill -3 <pid> or in IntelliJ: Run > Dump Threads",
        "// Read the thread dump — find BLOCKED threads and what they're waiting for"
      ]},
      { type: "p", value: "Reading a thread dump is a real production skill. This is the first time you do it deliberately. You will do it in production when a service hangs." },
      { type: "bold", value: "Docker basics (60 mins):" },
      { type: "p", value: "File: Dockerfile in project root" },
      { type: "code", lang: "dockerfile", lines: [
        "FROM eclipse-temurin:21-jdk-alpine",
        "WORKDIR /app",
        "COPY target/payment-wallet-1.0-SNAPSHOT.jar app.jar",
        "EXPOSE 8080",
        "ENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]"
      ]},
      { type: "p", value: "File: docker-compose.yml in project root" },
      { type: "code", lang: "yaml", lines: [
        "version: '3.8'",
        "services:",
        "  mysql:",
        "    image: mysql:8.0",
        "    environment:",
        "      MYSQL_ROOT_PASSWORD: root",
        "      MYSQL_DATABASE: wallet_db",
        "    ports: [\"3306:3306\"]",
        "    volumes: [\"mysql_data:/var/lib/mysql\"]",
        "  redis:",
        "    image: redis:7-alpine",
        "    ports: [\"6379:6379\"]",
        "volumes:",
        "  mysql_data:"
      ]},
      { type: "code", lang: "bash", lines: [
        "# Run it",
        "docker-compose up -d",
        "docker ps                          # verify both containers running",
        "docker logs payment-wallet-mysql-1 # read logs",
        "docker exec -it payment-wallet-mysql-1 mysql -u root -proot wallet_db"
      ]},
      { type: "bold", value: "Failure simulation:" },
      { type: "code", lang: "bash", lines: [
        "# Stop MySQL while it's running",
        "docker-compose stop mysql",
        "# Try to connect — observe connection refused",
        "# Start it again",
        "docker-compose start mysql",
        "# This is what happens in production. Know how to recover."
      ]},
      { type: "done", value: "docker-compose up -d starts MySQL and Redis. You read a thread dump and identify a deadlock." },
    ],
  },
  w3d19: {
    id: "w3d19", weekId: "w3", dayNumber: 19, dayLabel: "Friday", estimatedMinutes: 90,
    title: "ThreadPoolExecutor Deep + HTTP Fundamentals",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "ThreadPoolExecutor — what you must understand:" },
      { type: "bullet", value: "Why Executors factory methods hide important details — newCachedThreadPool() has unbounded thread creation. You cannot see or control the real parameters." },
      { type: "bullet", value: "corePoolSize — threads kept alive even when idle. Always available." },
      { type: "bullet", value: "maximumPoolSize — ceiling on threads ever created. Beyond this, tasks go to the queue." },
      { type: "bullet", value: "Queue — tasks wait here when all core threads are busy. ALWAYS use a bounded queue. Unbounded queue can grow without limit and cause OOM." },
      { type: "bullet", value: "RejectionPolicy — what happens when queue is full AND all max threads are busy:" },
      { type: "bullet", value: "  - CallerRunsPolicy — the thread that submitted the task executes it. Slows the producer. Never drops payments. Best for financial systems." },
      { type: "bullet", value: "  - AbortPolicy — throws RejectedExecutionException. Default behaviour. Drops the task." },
      { type: "bullet", value: "  - DiscardPolicy — silently discards the task. Never acceptable for financial data." },
      { type: "bullet", value: "  - DiscardOldestPolicy — drops the oldest queued task. Never acceptable for financial data." },
      { type: "bullet", value: "Pool sizing formula for IO-bound workloads — (CPU core count × 2) + 1" },
      { type: "empty", value: "" },
      { type: "bold", value: "HTTP fundamentals — what you must understand:" },
      { type: "bullet", value: "Full lifecycle of an HTTP request — DNS lookup → TCP 3-way handshake → TLS handshake (HTTPS) → HTTP request sent → server processes → HTTP response → connection reused or closed" },
      { type: "bullet", value: "What the TCP 3-way handshake is — SYN, SYN-ACK, ACK. Required before any data can be exchanged." },
      { type: "bullet", value: "What TLS handshake does — negotiates encryption algorithm and exchanges keys. Adds multiple round trips. Expensive." },
      { type: "bullet", value: "Why connection pooling exists — creating a new TCP+TLS connection for every single DB or HTTP request is too expensive. Reuse existing connections." },
      { type: "bullet", value: "How HTTP keep-alive works — the connection stays open after the response is sent. The next request reuses it without a new handshake." },
      { type: "bullet", value: "HikariCP for DB and HttpClient pool for HTTP serve the same purpose — both avoid expensive connection creation on every request" },
      { type: "tip", value: "curl -v https://httpbin.org/get shows every step of the HTTP conversation. Run it and read every single line of output." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "ThreadPoolExecutor (40 mins):" },
      { type: "p", value: "Executors factory methods hide the real class. In production, configure ThreadPoolExecutor directly so you know exactly what will happen under load." },
      { type: "code", lang: "java", lines: [
        "ThreadPoolExecutor executor = new ThreadPoolExecutor(",
        "    10,                              // corePoolSize: threads always alive",
        "    50,                              // maximumPoolSize: max threads ever",
        "    60L, TimeUnit.SECONDS,           // idle thread keepalive",
        "    new ArrayBlockingQueue<>(1000),  // BOUNDED queue — never use unbounded",
        "    r -> { Thread t = new Thread(r);",
        "           t.setName(\"wallet-worker-\" + t.getId()); return t; },",
        "    new ThreadPoolExecutor.CallerRunsPolicy()",
        ");"
      ]},
      { type: "p", value: "Rejection policies for a payment system:" },
      { type: "bullet", value: "CallerRunsPolicy: calling thread executes the task — slows down producer, never drops payments" },
      { type: "bullet", value: "AbortPolicy: throws exception — default, but loses the payment" },
      { type: "bullet", value: "DiscardPolicy: silently drops — never use for financial data" },
      { type: "bullet", value: "DiscardOldestPolicy: drops oldest queued item — never use for financial data" },
      { type: "bold", value: "HTTP fundamentals (50 mins) — before you build REST APIs you must know this:" },
      { type: "p", value: "Every HTTP request follows this path: DNS lookup → TCP handshake → TLS handshake (for HTTPS) → HTTP request → server processes → HTTP response → connection kept alive or closed." },
      { type: "code", lang: "bash", lines: [
        "# Trace it yourself with curl",
        "curl -v https://httpbin.org/get",
        "# Watch: * Trying IP... TCP connect",
        "# * SSL handshake                  TLS",
        "# > GET /get HTTP/1.1              request headers",
        "# < HTTP/1.1 200 OK                response status",
        "# < Content-Type: application/json response headers",
        "# {\"json\": body}                  response body"
      ]},
      { type: "p", value: "Connection pooling: creating a TCP+TLS connection for every request is expensive. Connection pools reuse existing connections. HikariCP does this for database. HttpClient does this for HTTP. Never create new connections per request." },
      { type: "tip", value: "The same principle that makes HikariCP worth using for database connections applies to external HTTP calls. Both are expensive to create." },
      { type: "done", value: "You configure ThreadPoolExecutor from scratch. You trace an HTTP request end to end with curl -v." },
    ],
  },
  w3d20: {
    id: "w3d20", weekId: "w3", dayNumber: 20, dayLabel: "Saturday", estimatedMinutes: 150,
    title: "Week 3 Integration — Concurrent Batch Processor + Debugging",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "What to understand before the integration day:" },
      { type: "bullet", value: "How Week 3 pieces connect — ExecutorService manages threads, CompletableFuture chains async steps, BalanceManager uses CAS, PaymentBatchProcessor ties all of them together" },
      { type: "bullet", value: "What a conditional breakpoint is — a breakpoint that only pauses execution when a boolean condition is true (e.g. balance < 0 after debit)" },
      { type: "bullet", value: "Why conditional breakpoints are essential for concurrency debugging — you cannot step through 100 concurrent threads manually" },
      { type: "bullet", value: "The debugging approach — introduce the race condition deliberately, set a conditional breakpoint on the wrong state, run the concurrent test, let the bug surface" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Build the batch processor:" },
      { type: "p", value: "File: src/main/java/com/wallet/service/PaymentBatchProcessor.java" },
      { type: "code", lang: "java", lines: [
        "public class PaymentBatchProcessor {",
        "    private static final Logger log = LoggerFactory.getLogger(PaymentBatchProcessor.class);",
        "    private final ThreadPoolExecutor executor;",
        "    private final PaymentContext context;",
        "    private final BalanceManager balanceManager;",
        "",
        "    public PaymentBatchProcessor(PaymentContext context, BalanceManager bm) {",
        "        this.context = context;",
        "        this.balanceManager = bm;",
        "        this.executor = new ThreadPoolExecutor(",
        "            5, 20, 60L, TimeUnit.SECONDS,",
        "            new ArrayBlockingQueue<>(500),",
        "            r -> new Thread(r, \"batch-worker\"),",
        "            new ThreadPoolExecutor.CallerRunsPolicy());",
        "    }",
        "",
        "    public List<CompletableFuture<String>> submitBatch(List<PaymentRequest> requests) {",
        "        return requests.stream()",
        "            .map(req -> CompletableFuture.supplyAsync(",
        "                () -> {",
        "                    boolean debited = balanceManager.debit(req.getUserId(), (long) req.getAmount());",
        "                    if (!debited) return \"INSUFFICIENT_FUNDS\";",
        "                    return context.execute(req);",
        "                },",
        "                executor))",
        "            .collect(Collectors.toList());",
        "    }",
        "}"
      ]},
      { type: "bold", value: "Hour 2 — Failure simulation + debugging:" },
      { type: "p", value: "Submit 100 payments with 10 users, each starting with balance 1000. Each payment debits 100. Expected: each user's balance = 0 after 10 payments. Verify this is correct under concurrent execution." },
      { type: "code", lang: "java", lines: [
        "// Concurrency test",
        "@Test",
        "void batchProcessor_100ConcurrentPayments_correctFinalBalances() throws Exception {",
        "    // setup 10 users with 1000 balance each",
        "    // submit 100 payments (10 per user, 100 each)",
        "    // wait for all futures",
        "    // assert each user balance == 0",
        "}"
      ]},
      { type: "bold", value: "Hour 3 — Debugging exercise:" },
      { type: "p", value: "Introduce a deliberate bug: remove the CAS loop from BalanceManager.debit() and replace with a simple get/set. Run the concurrency test. Watch it fail. Use IntelliJ debugger with a conditional breakpoint on the wrong balance value. Find exactly which thread causes the corruption. Restore CAS. Verify test passes." },
      { type: "done", value: "Batch processor handles 100 concurrent payments correctly. Concurrency test passes. You debugged a race condition with a conditional breakpoint." },
    ],
  },
  w3d21: {
    id: "w3d21", weekId: "w3", dayNumber: 21, dayLabel: "Sunday", estimatedMinutes: 150,
    title: "Week 3 Revision",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Answer every question from memory. No notes. No IDE." },
      { type: "bullet", value: "What is a race condition? Show one in code and fix it three different ways." },
      { type: "bullet", value: "Why is volatile not enough for count++? What is happening at the CPU level?" },
      { type: "bullet", value: "What is a deadlock? How do you prevent one in a money transfer between two accounts?" },
      { type: "bullet", value: "What is thenCompose vs thenCombine? When exactly do you use each?" },
      { type: "bullet", value: "What happens when all threads in a ThreadPoolExecutor are busy and the queue is full?" },
      { type: "bullet", value: "You need to process 1000 payment requests per second. Walk me through your design approach." },
      { type: "bullet", value: "How do you read a thread dump? What do BLOCKED and WAITING states tell you?" },
      { type: "warn", value: "If you cannot answer these questions confidently — do not move to Week 4." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Write from memory:" },
      { type: "bullet", value: "Thread lifecycle states" },
      { type: "bullet", value: "synchronized vs volatile vs AtomicInteger — exact rules for each" },
      { type: "bullet", value: "CompletableFuture: thenApply vs thenCompose vs thenCombine — when to use each" },
      { type: "bullet", value: "How to read a thread dump — what BLOCKED and WAITING mean" },
      { type: "bold", value: "Hour 2 — Interview questions out loud:" },
      { type: "bullet", value: "What is a race condition? Show me one in code and fix it." },
      { type: "bullet", value: "Why is volatile not enough for count++?" },
      { type: "bullet", value: "What is a deadlock? How do you prevent it in a money transfer?" },
      { type: "bullet", value: "What happens when all threads are busy and the queue is full?" },
      { type: "bullet", value: "You need to process 1000 payment requests per second. Walk me through your approach." },
      { type: "bold", value: "Hour 3 — Read Docker logs from your running containers:" },
      { type: "code", lang: "bash", lines: [
        "docker logs -f payment-wallet-mysql-1   # follow logs in real time",
        "docker stats                             # CPU and memory usage",
        "docker exec -it payment-wallet-redis-1 redis-cli ping"
      ]},
      { type: "warn", value: "If you cannot answer Hour 2 questions confidently, do not move to Week 4." },
    ],
  },
};

export default WEEK3_TOPICS;
