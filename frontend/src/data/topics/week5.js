// Topic content for Week 5 — JPA + SQL Depth + Indexes + Schema Patterns

const WEEK5_TOPICS = {
  w5d29: {
    id: "w5d29", weekId: "w5", dayNumber: 29, dayLabel: "Monday", estimatedMinutes: 90,
    title: "JPA Entities + Hibernate Internals",
    subtitle: "Project: domain/model/",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "N+1 problem — what you must understand:" },
      { type: "bullet", value: "What N+1 is — 1 query fetches N parent entities, then N more queries fetch their child associations. Total: N+1 queries." },
      { type: "bullet", value: "Why it is dangerous — fetching 1000 accounts executes 1001 queries. Performance degrades linearly with data size." },
      { type: "bullet", value: "What lazy loading is — associations are not loaded until you access them in code. Default for @OneToMany collections." },
      { type: "bullet", value: "What eager loading is — associations loaded immediately in the same query as the parent. Default for @ManyToOne." },
      { type: "bullet", value: "How to fix N+1 — use JOIN FETCH in JPQL query or @EntityGraph on the repository method to load in one query" },
      { type: "empty", value: "" },
      { type: "bold", value: "Optimistic locking with @Version — what you must understand:" },
      { type: "bullet", value: "What optimistic locking assumes — conflicts are rare. Read freely without locking. Check at write time that nothing changed." },
      { type: "bullet", value: "What the @Version field does — JPA automatically increments it on every UPDATE. If the version in the DB does not match what you read, throw OptimisticLockException." },
      { type: "bullet", value: "The lost update problem without @Version — Thread 1 reads version=5. Thread 2 reads version=5. Thread 1 writes (now version=6). Thread 2 writes based on stale version=5, overwriting Thread 1\'s change." },
      { type: "bullet", value: "With @Version — Thread 2\'s UPDATE affects 0 rows (version=5 no longer exists, it is 6). JPA throws OptimisticLockException. Thread 2 must retry." },
      { type: "bullet", value: "Optimistic vs pessimistic locking — optimistic: higher throughput, occasional retry. Pessimistic (FOR UPDATE): lower throughput, no retry needed. Use optimistic for most payment scenarios." },
      { type: "empty", value: "" },
      { type: "bold", value: "LazyInitializationException — what you must understand:" },
      { type: "bullet", value: "What causes it — accessing a lazy-loaded association AFTER the Hibernate session has closed" },
      { type: "bullet", value: "When the session closes — @Transactional opens a Hibernate session for the duration of the method. Session closes when the method exits." },
      { type: "bullet", value: "Why accessing the lazy field outside the transaction fails — no open session to execute the SQL query for the association" },
      { type: "bullet", value: "Fix 1 — add @Transactional to the service method to keep the session alive through the association access" },
      { type: "bullet", value: "Fix 2 — use JOIN FETCH in the repository query to load the association eagerly in one query before the session closes" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "40 mins" },
      { type: "bold", value: "N+1 problem — the most common JPA performance mistake:" },
      { type: "code", lang: "java", lines: [
        "// BAD — 1 query for accounts + N queries for payments",
        "List<Account> accounts = accountRepo.findAll();",
        "for (Account a : accounts) {",
        "    a.getPayments(); // N separate queries!",
        "}",
        "",
        "// FIX 1 — JOIN FETCH: 1 query",
        "@Query(\"SELECT a FROM Account a LEFT JOIN FETCH a.payments\")",
        "List<Account> findAllWithPayments();",
        "",
        "// FIX 2 — @EntityGraph",
        "@EntityGraph(attributePaths = {\"payments\"})",
        "List<Account> findAll();"
      ]},
      { type: "exercise", label: "Build JPA entities — 50 mins" },
      { type: "p", value: "File: src/main/java/com/wallet/domain/model/Account.java" },
      { type: "code", lang: "java", lines: [
        "@Entity @Table(name = \"accounts\")",
        "public class Account {",
        "    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)",
        "    private Long id;",
        "    @Column(nullable = false)",
        "    private String userId;",
        "    @Column(nullable = false, precision = 15, scale = 4)",
        "    private BigDecimal balance = BigDecimal.ZERO;",
        "    @Version  // JPA manages this — prevents lost updates",
        "    private Long version;",
        "    @CreationTimestamp",
        "    private LocalDateTime createdAt;",
        "    @Column",
        "    private LocalDateTime deletedAt; // soft delete — never hard delete",
        "}"
      ]},
      { type: "bold", value: "Failure simulation — LazyInitializationException:" },
      { type: "code", lang: "java", lines: [
        "// In a service method WITHOUT @Transactional:",
        "Account account = accountRepo.findById(1L).get(); // session opens and closes",
        "account.getPayments(); // session CLOSED — LazyInitializationException",
        "// Fix 1: add @Transactional to the service method",
        "// Fix 2: use JOIN FETCH in the repository query"
      ]},
      { type: "p", value: "Build this failure deliberately. Read the stack trace. Understand exactly which line throws and why. Then apply both fixes and verify each works." },
      { type: "done", value: "JPA entities exist. You reproduce and fix LazyInitializationException. You explain N+1 and fix it." },
    ],
  },
  w5d30: {
    id: "w5d30", weekId: "w5", dayNumber: 30, dayLabel: "Tuesday", estimatedMinutes: 90,
    title: "B-Tree Indexes + EXPLAIN Output",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "How B-Tree indexes work internally — what you must understand:" },
      { type: "bullet", value: "Without index — finding a row by email in 1 million rows scans all 1 million rows. O(n)." },
      { type: "bullet", value: "With B-Tree index — 3-4 disk reads maximum regardless of table size. O(log n). This is the single biggest performance lever in backend." },
      { type: "bullet", value: "What a B-Tree physically is — a balanced tree of sorted keys. Leaf nodes point to row data." },
      { type: "bullet", value: "Clustered index (InnoDB primary key) — the leaf node IS the row data. The table IS the B-Tree. No second lookup needed." },
      { type: "bullet", value: "Secondary index — leaf node holds (indexed value, primary key). Finding the row requires two B-Tree traversals." },
      { type: "bullet", value: "What a covering index is — an index that includes all columns the query needs. Eliminates the second lookup to the clustered index entirely." },
      { type: "empty", value: "" },
      { type: "bold", value: "Composite index rules — what you must understand:" },
      { type: "bullet", value: "Leftmost prefix rule — index on (user_id, status, created_at) is usable for: user_id alone, user_id+status, or all three. NOT for status alone or created_at alone." },
      { type: "bullet", value: "Why leftmost prefix — the B-Tree is sorted first by user_id, then by status, then by created_at. Skipping user_id means the tree cannot be navigated." },
      { type: "bullet", value: "Column order rule — equality filter columns first, range filter columns last. (user_id, status) for equality, then created_at for range." },
      { type: "bullet", value: "Why wrapping a column in a function destroys index usage — WHERE YEAR(created_at) = 2024 computes a new value per row. The index on created_at cannot be used." },
      { type: "empty", value: "" },
      { type: "bold", value: "EXPLAIN output — what each field means:" },
      { type: "bullet", value: "type=ALL — full table scan. Every single row is read. The worst case. Fix it with an index." },
      { type: "bullet", value: "type=ref — index used. Only rows matching the condition are read. Good." },
      { type: "bullet", value: "type=const — primary key or unique key lookup. Exactly one row found. Best case." },
      { type: "bullet", value: "key column — which index MySQL chose to use. NULL means no index was used." },
      { type: "bullet", value: "rows column — estimated number of rows MySQL will examine. High = slow." },
      { type: "bullet", value: "Extra column — \"Using filesort\" means ORDER BY cannot use an index. \"Using index\" means a covering index was used." },
      { type: "tip", value: "Record real numbers: \"Before index: 780ms. After index: 11ms on 100k rows.\" These specific numbers impress interviewers." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "55 mins" },
      { type: "p", value: "Without index: finding user by email in 1 million rows = scan all 1 million = O(n). B-Tree index = 3-4 disk reads = O(log n). This is the single biggest performance lever in backend engineering." },
      { type: "p", value: "InnoDB clustered vs non-clustered:" },
      { type: "bullet", value: "Primary key = clustered index. Row data IS the leaf node. The table IS the B-Tree." },
      { type: "bullet", value: "Secondary index leaf = (indexed value, primary key). Finding a row needs secondary search → get PK → clustered search. Two lookups." },
      { type: "bullet", value: "Covering index includes all columns the query needs — eliminates second lookup entirely" },
      { type: "p", value: "Composite index leftmost prefix rule:" },
      { type: "code", lang: "sql", lines: [
        "-- Index: (user_id, status, created_at)",
        "-- CAN use: WHERE user_id = 1",
        "-- CAN use: WHERE user_id = 1 AND status = 'SUCCESS'",
        "-- CANNOT:  WHERE status = 'SUCCESS'         -- skips leftmost",
        "-- CANNOT:  WHERE created_at > '2024-01-01'  -- skips first two",
        "-- Rule: equality columns first, range columns last"
      ]},
      { type: "exercise", label: "30 mins — run EXPLAIN on real data" },
      { type: "code", lang: "sql", lines: [
        "-- First insert 100k rows into your Docker MySQL",
        "EXPLAIN SELECT * FROM payments WHERE status = 'SUCCESS';",
        "-- type=ALL means full table scan — fix it",
        "CREATE INDEX idx_status ON payments(status);",
        "EXPLAIN SELECT * FROM payments WHERE status = 'SUCCESS';",
        "-- type=ref means index used — measure the difference"
      ]},
      { type: "p", value: "Record your real numbers: 'Before index: Xms. After index: Yms on 100k rows.' You will say these in interviews." },
      { type: "bold", value: "Failure simulation — function on column:" },
      { type: "code", lang: "sql", lines: [
        "WHERE YEAR(created_at) = 2024;  -- cannot use index — full scan",
        "WHERE created_at >= '2024-01-01' AND created_at < '2025-01-01'; -- uses index"
      ]},
      { type: "p", value: "Run EXPLAIN on both. See the difference. Understand why wrapping a column in a function destroys index usage." },
      { type: "done", value: "You read EXPLAIN output and identify full table scans. You have real performance numbers." },
    ],
  },
  w5d31: {
    id: "w5d31", weekId: "w5", dayNumber: 31, dayLabel: "Wednesday", estimatedMinutes: 90,
    title: "ACID + Isolation Levels + Locking",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "ACID — how MySQL actually implements each property:" },
      { type: "bullet", value: "Atomicity — UNDO LOG. Before any data change, the old value is written to the undo log. Rollback = apply undo log in reverse order." },
      { type: "bullet", value: "Consistency — constraints (NOT NULL, UNIQUE, FOREIGN KEY) enforced together with application-level rules in one atomic operation" },
      { type: "bullet", value: "Isolation — configurable via isolation level. See below." },
      { type: "bullet", value: "Durability — REDO LOG (Write-Ahead Log). Committed data is written to the redo log before returning success to the client. Survives crashes." },
      { type: "empty", value: "" },
      { type: "bold", value: "All 4 isolation levels — what each prevents:" },
      { type: "bullet", value: "READ UNCOMMITTED — you can read another transaction\'s uncommitted (not yet finalized) data. Allows dirty reads. Never use in financial systems." },
      { type: "bullet", value: "READ COMMITTED — no dirty reads. You only see committed data. Allows non-repeatable reads (same row read twice may return different values). PostgreSQL default." },
      { type: "bullet", value: "REPEATABLE READ — no dirty reads, no non-repeatable reads. The same row read twice in a transaction always returns the same value. MySQL InnoDB default." },
      { type: "bullet", value: "SERIALIZABLE — full isolation. Transactions run as if they are sequential. Slowest. Use only for critical uniqueness checks (promo code redemption)." },
      { type: "empty", value: "" },
      { type: "bold", value: "Locking — what you must understand:" },
      { type: "bullet", value: "FOR SHARE — shared read lock. Blocks other writers, allows other readers. Use when reading data that must not be modified until your read completes." },
      { type: "bullet", value: "FOR UPDATE — exclusive lock. Blocks all other readers and writers. Use when you will definitely update the row based on what you read." },
      { type: "bullet", value: "Why FOR UPDATE is critical before a balance debit — without it, another transaction can change the balance between your SELECT and your UPDATE" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "50 mins" },
      { type: "bold", value: "How MySQL implements ACID:" },
      { type: "bullet", value: "Atomicity: UNDO LOG — before any change, old value written here. Rollback = apply undo log in reverse." },
      { type: "bullet", value: "Consistency: constraints (NOT NULL, UNIQUE, FK) + application rules" },
      { type: "bullet", value: "Isolation: configurable via isolation level (see below)" },
      { type: "bullet", value: "Durability: REDO LOG (Write-Ahead Log) — committed data survives crashes" },
      { type: "bold", value: "Isolation levels — know all four:" },
      { type: "code", lang: "sql", lines: [
        "-- READ UNCOMMITTED: sees other tx's uncommitted data — never use",
        "-- READ COMMITTED:   no dirty reads. PostgreSQL default. Good for most fintech ops.",
        "-- REPEATABLE READ:  no dirty or non-repeatable reads. MySQL InnoDB default.",
        "-- SERIALIZABLE:     full isolation. Slowest. Use only for promo-code uniqueness."
      ]},
      { type: "exercise", label: "Locking in practice — 40 mins" },
      { type: "code", lang: "sql", lines: [
        "-- FOR SHARE: read and block writers (allow other readers)",
        "SELECT * FROM accounts WHERE id = 1 FOR SHARE;",
        "",
        "-- FOR UPDATE: read and block everyone (exclusive)",
        "-- Use when: you read a value and WILL update based on it",
        "-- Without it: another tx can change the value between your read and write",
        "SELECT * FROM accounts WHERE id = 1 FOR UPDATE;"
      ]},
      { type: "bold", value: "Failure simulation — dirty read:" },
      { type: "code", lang: "sql", lines: [
        "-- Open two MySQL sessions",
        "-- Session 1: SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED; BEGIN;",
        "-- Session 1: UPDATE accounts SET balance = 99999 WHERE id = 1;",
        "-- Session 1: (DO NOT COMMIT)",
        "-- Session 2: SELECT balance FROM accounts WHERE id = 1;",
        "-- Session 2 sees 99999 — a dirty read",
        "-- Session 1: ROLLBACK",
        "-- Session 2: SELECT again — sees original value"
      ]},
      { type: "p", value: "This is why READ UNCOMMITTED is never used in financial systems." },
      { type: "done", value: "You name all 4 isolation levels and what each prevents. You explain when to use FOR UPDATE vs FOR SHARE." },
    ],
  },
  w5d32: {
    id: "w5d32", weekId: "w5", dayNumber: 32, dayLabel: "Thursday", estimatedMinutes: 90,
    title: "Fintech Schema Patterns",
    subtitle: "Project: db/migration/",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Pattern 1 — Idempotency at DB level:" },
      { type: "bullet", value: "What it solves — client retries a payment. Without this pattern, two identical payments are created." },
      { type: "bullet", value: "How it works — UNIQUE constraint on the idempotency_key column in the payments table" },
      { type: "bullet", value: "ON DUPLICATE KEY UPDATE id=id — on duplicate insert, silently do nothing. No error thrown. No new row created." },
      { type: "bullet", value: "Why this is necessary alongside Redis idempotency — defense in depth. Redis can fail or expire. The DB constraint is permanent." },
      { type: "empty", value: "" },
      { type: "bold", value: "Pattern 2 — Optimistic locking with version column:" },
      { type: "bullet", value: "What it solves — two concurrent updates to the same balance row, one must fail and retry" },
      { type: "bullet", value: "How it works — UPDATE accounts SET balance=..., version=version+1 WHERE id=? AND version=? — if 0 rows affected, someone else updated it first" },
      { type: "empty", value: "" },
      { type: "bold", value: "Pattern 3 — Soft delete:" },
      { type: "bullet", value: "What it solves — financial records must never be permanently deleted. Audit trail, regulatory compliance." },
      { type: "bullet", value: "How it works — deleted_at TIMESTAMP NULL column. \"Delete\" = SET deleted_at=NOW(). All queries add WHERE deleted_at IS NULL." },
      { type: "bullet", value: "Why hard DELETE is never used in fintech — you lose the audit trail and may violate regulations (RBI, SEBI)" },
      { type: "empty", value: "" },
      { type: "bold", value: "Pattern 4 — Cursor-based pagination:" },
      { type: "bullet", value: "What OFFSET does — scans all N preceding rows on every page to skip them. Page 1000 with 20 items scans 20,000 rows. Gets slower with every page." },
      { type: "bullet", value: "What cursor-based does — WHERE id < lastSeenId ORDER BY id DESC LIMIT 20. Uses the primary key index. Always O(log n) regardless of page number." },
      { type: "bullet", value: "Why cursor pagination scales — page 1 and page 100,000 take exactly the same time" },
      { type: "bullet", value: "The tradeoff — cannot jump to an arbitrary page number. Works for infinite scroll, not traditional numbered pages." },
      { type: "tip", value: "Know why each of the 4 patterns exists. Not just what it does — the specific problem it prevents." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "study", value: "All 4 patterns — these appear directly in your schema" },
      { type: "bold", value: "Pattern 1 — Idempotency at DB level:" },
      { type: "code", lang: "sql", lines: [
        "-- payments table has UNIQUE constraint on idempotency_key",
        "INSERT INTO payments (idempotency_key, from_account, to_account, amount)",
        "VALUES ('client-uuid-123', 1, 2, 500.00)",
        "ON DUPLICATE KEY UPDATE id=id; -- duplicate = silently do nothing"
      ]},
      { type: "bold", value: "Pattern 2 — Optimistic locking:" },
      { type: "code", lang: "sql", lines: [
        "UPDATE accounts",
        "SET balance = balance - 500, version = version + 1",
        "WHERE id = 1 AND version = 5;",
        "-- 0 rows affected = someone else updated it. Fetch fresh data. Retry."
      ]},
      { type: "bold", value: "Pattern 3 — Soft delete:" },
      { type: "code", lang: "sql", lines: [
        "-- Never hard DELETE financial records",
        "UPDATE payments SET deleted_at = NOW() WHERE id = 123;",
        "-- All queries add: WHERE deleted_at IS NULL"
      ]},
      { type: "bold", value: "Pattern 4 — Cursor-based pagination:" },
      { type: "code", lang: "sql", lines: [
        "-- BAD: OFFSET 10000 scans 10020 rows to return 20. Slower every page.",
        "SELECT * FROM payments ORDER BY id DESC LIMIT 20 OFFSET 10000;",
        "",
        "-- GOOD: always O(log n) via primary key index. Page 1 = Page 100000 speed.",
        "SELECT * FROM payments",
        "WHERE id < :lastSeenId  -- cursor from previous page",
        "ORDER BY id DESC LIMIT 20;"
      ]},
      { type: "exercise", label: "Build the full schema — 50 mins" },
      { type: "code", lang: "sql", lines: [
        "-- File: src/main/resources/db/migration/V1__initial_schema.sql",
        "CREATE TABLE users (",
        "    id BIGINT AUTO_INCREMENT PRIMARY KEY,",
        "    email VARCHAR(255) UNIQUE NOT NULL,",
        "    password_hash VARCHAR(255) NOT NULL,",
        "    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,",
        "    deleted_at TIMESTAMP NULL,",
        "    INDEX idx_email (email)",
        ");",
        "CREATE TABLE accounts (",
        "    id BIGINT AUTO_INCREMENT PRIMARY KEY,",
        "    user_id BIGINT NOT NULL,",
        "    balance DECIMAL(15,4) NOT NULL DEFAULT 0,",
        "    version BIGINT NOT NULL DEFAULT 0,",
        "    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,",
        "    deleted_at TIMESTAMP NULL,",
        "    FOREIGN KEY (user_id) REFERENCES users(id),",
        "    INDEX idx_user_id (user_id)",
        ");",
        "CREATE TABLE payments (",
        "    id BIGINT AUTO_INCREMENT PRIMARY KEY,",
        "    idempotency_key VARCHAR(100) UNIQUE NOT NULL,",
        "    from_account_id BIGINT NOT NULL,",
        "    to_account_id BIGINT NOT NULL,",
        "    amount DECIMAL(15,4) NOT NULL,",
        "    status ENUM('PENDING','PROCESSING','SUCCESS','FAILED') DEFAULT 'PENDING',",
        "    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,",
        "    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,",
        "    deleted_at TIMESTAMP NULL,",
        "    INDEX idx_from_account_created (from_account_id, created_at),",
        "    INDEX idx_status_created (status, created_at)",
        ");"
      ]},
      { type: "done", value: "Schema is in Flyway migration. All 4 fintech patterns applied. You explain cursor-based pagination vs OFFSET." },
    ],
  },
  w5d33: {
    id: "w5d33", weekId: "w5", dayNumber: 33, dayLabel: "Friday", estimatedMinutes: 90,
    title: "HikariCP + System Design Framework — Rate Limiter Design",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "HikariCP — what you must understand:" },
      { type: "bullet", value: "What a connection pool is — a set of pre-created database connections that are reused across requests instead of created fresh each time" },
      { type: "bullet", value: "Why creating a new DB connection per request is too expensive — TCP handshake + authentication + memory allocation on every single request" },
      { type: "bullet", value: "minimum-idle — connections kept alive and ready even when traffic is zero" },
      { type: "bullet", value: "maximum-pool-size — the ceiling. More connections is NOT always better. OS context switching overhead increases." },
      { type: "bullet", value: "connection-timeout — how long a request waits for an available connection before receiving an exception" },
      { type: "bullet", value: "Pool sizing formula — (CPU core count × 2) + 1 for IO-bound workloads. A 4-core server needs ~9 connections." },
      { type: "bullet", value: "What connection pool exhaustion looks like — all connections checked out, new requests time out waiting. Cascading failure pattern." },
      { type: "empty", value: "" },
      { type: "bold", value: "System Design 7-step framework — use this for every system design interview:" },
      { type: "bullet", value: "Step 1 — Clarify requirements (5 min): who uses it, what are the scale numbers, what is the latency budget, what consistency is required" },
      { type: "bullet", value: "Step 2 — Estimate scale (5 min): requests per second, storage per year, peak traffic multiplier, bandwidth" },
      { type: "bullet", value: "Step 3 — High-level design (10 min): draw the main components — load balancer, app servers, database, cache, queues" },
      { type: "bullet", value: "Step 4 — API design (5 min): define the main endpoints, request and response shapes" },
      { type: "bullet", value: "Step 5 — Data model (5 min): key tables or data structures, important fields and indexes" },
      { type: "bullet", value: "Step 6 — Deep dive (20 min): the interviewer will steer this. Go deep on the hardest or most interesting part." },
      { type: "bullet", value: "Step 7 — Bottlenecks (5 min): what breaks at 10x the estimated load and how you would fix each thing" },
      { type: "tip", value: "Never jump to a solution before clarifying requirements. Interviewers specifically penalise this." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "HikariCP (30 mins):" },
      { type: "code", lang: "properties", lines: [
        "spring.datasource.hikari.minimum-idle=5",
        "spring.datasource.hikari.maximum-pool-size=20",
        "spring.datasource.hikari.connection-timeout=30000",
        "spring.datasource.hikari.idle-timeout=300000",
        "spring.datasource.hikari.max-lifetime=1800000"
      ]},
      { type: "p", value: "Pool sizing formula: connections = (core_count × 2) + 1. A 4-core server needs ~9 connections. More connections is NOT always better — OS context switching overhead increases." },
      { type: "bold", value: "Failure simulation:" },
      { type: "code", lang: "bash", lines: [
        "# Set maximum-pool-size=1 in application.yml",
        "# Fire 10 concurrent requests",
        "# Watch: connection-timeout exception for 9 of them",
        "# Set back to 20. All 10 succeed.",
        "# This is how connection pool exhaustion happens in production."
      ]},
      { type: "bold", value: "System Design Framework — Rate Limiter (60 mins):" },
      { type: "p", value: "Use this framework for every system design question:" },
      { type: "bullet", value: "Step 1 Clarify (5 min): who, scale, latency budget, consistency needs" },
      { type: "bullet", value: "Step 2 Estimate (5 min): req/sec, storage/year, peak multiplier" },
      { type: "bullet", value: "Step 3 High-level (10 min): draw the boxes — LB, app servers, DB, cache" },
      { type: "bullet", value: "Step 4 API design (5 min): main endpoints" },
      { type: "bullet", value: "Step 5 Data model (5 min): key tables or data structures" },
      { type: "bullet", value: "Step 6 Deep dive (20 min): interviewer steers this" },
      { type: "bullet", value: "Step 7 Bottlenecks (5 min): what breaks at 10x and how to fix it" },
      { type: "p", value: "Design a rate limiter in your notebook (45 min):" },
      { type: "p", value: "Requirements: 10 req/min per user, distributed (multiple app servers), < 5ms latency added." },
      { type: "p", value: "Scale: 1M users × 10 req/min = 167k req/sec peak." },
      { type: "p", value: "Architecture: Client → LB → App Server → Rate Limiter Middleware → Redis Cluster." },
      { type: "p", value: "Algorithm: sliding window counter in Redis using Lua script (atomic INCR + EXPIRE in one round trip)." },
      { type: "p", value: "Failure mode: Redis down → fail open. Better to let extra requests through than block all payments." },
      { type: "done", value: "You deliver the rate limiter design in 45 minutes using the framework. You explain HikariCP pool sizing." },
    ],
  },
  w5d34: {
    id: "w5d34", weekId: "w5", dayNumber: 34, dayLabel: "Saturday", estimatedMinutes: 150,
    title: "Week 5 Integration — 100k rows + EXPLAIN + Optimistic Locking",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "What to understand before this day:" },
      { type: "bullet", value: "How to insert bulk data efficiently — stored procedure with a WHILE loop. Run in one transaction for speed." },
      { type: "bullet", value: "What SQL_NO_CACHE does — bypasses MySQL\'s query cache so you measure real disk + index performance, not cached results" },
      { type: "bullet", value: "Why recording before/after numbers matters — \"index improved this query from 780ms to 11ms on 100k rows\" is a specific, credible interview answer" },
      { type: "bullet", value: "How OptimisticLockException surfaces in a concurrent test — two threads UPDATE the same row, one gets 0 rows affected, JPA throws the exception" },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Insert 100k rows into Docker MySQL:" },
      { type: "code", lang: "sql", lines: [
        "-- Run this in mysql docker container",
        "DELIMITER //",
        "CREATE PROCEDURE insert_payments(IN n INT)",
        "BEGIN",
        "  DECLARE i INT DEFAULT 0;",
        "  WHILE i < n DO",
        "    INSERT INTO payments (idempotency_key, from_account_id, to_account_id, amount, status)",
        "    VALUES (UUID(), FLOOR(1 + RAND()*100), FLOOR(1 + RAND()*100),",
        "            RAND()*10000, ELT(FLOOR(1+RAND()*4),'PENDING','PROCESSING','SUCCESS','FAILED'));",
        "    SET i = i + 1;",
        "  END WHILE;",
        "END //",
        "DELIMITER ;",
        "CALL insert_payments(100000);"
      ]},
      { type: "bold", value: "Hour 2 — EXPLAIN every query, record real numbers:" },
      { type: "code", lang: "sql", lines: [
        "-- Before any index on status",
        "EXPLAIN SELECT * FROM payments WHERE status = 'SUCCESS'; -- type=ALL",
        "",
        "-- Add index",
        "CREATE INDEX idx_status ON payments(status);",
        "EXPLAIN SELECT * FROM payments WHERE status = 'SUCCESS'; -- type=ref",
        "",
        "-- Measure actual time",
        "SELECT SQL_NO_CACHE * FROM payments WHERE status = 'SUCCESS' LIMIT 20;",
        "-- Record: Before index Xms, after index Yms"
      ]},
      { type: "bold", value: "Hour 3 — Test optimistic locking:" },
      { type: "code", lang: "java", lines: [
        "@Test",
        "void transfer_concurrentDebits_optimisticLockPreventsLostUpdate() throws Exception {",
        "    // Two threads try to debit the same account simultaneously",
        "    // One should succeed, one should get OptimisticLockException",
        "    // Verify final balance is correct",
        "}"
      ]},
      { type: "done", value: "You have real before/after performance numbers. Optimistic locking test passes." },
    ],
  },
  w5d35: {
    id: "w5d35", weekId: "w5", dayNumber: 35, dayLabel: "Sunday", estimatedMinutes: 150,
    title: "Week 5 Revision",
    subtitle: "",
    content: [
      { type: "p", value: "📚 Study Guide:" },
      { type: "bold", value: "Answer every question from memory. No notes. No IDE." },
      { type: "bullet", value: "How does MySQL physically find a row when you query by email with a B-Tree index?" },
      { type: "bullet", value: "EXPLAIN shows type=ALL — what does this mean and exactly how do you fix it?" },
      { type: "bullet", value: "What is a covering index and when would you specifically create one?" },
      { type: "bullet", value: "List all 4 isolation levels and what each prevents" },
      { type: "bullet", value: "Why is OFFSET pagination dangerous on large tables? What do you use instead?" },
      { type: "bullet", value: "Optimistic vs pessimistic locking — when do you choose each in a payment system?" },
      { type: "warn", value: "If you cannot answer confidently — do not move to Week 6." },
      { type: "empty", value: "" },
      { type: "empty", value: "" },
      { type: "bold", value: "Hour 1 — Write from memory:" },
      { type: "bullet", value: "B-Tree structure — how MySQL finds a row" },
      { type: "bullet", value: "All 4 isolation levels and what each prevents" },
      { type: "bullet", value: "All 4 fintech schema patterns" },
      { type: "bullet", value: "Why OFFSET is dangerous and what to use instead" },
      { type: "bold", value: "Hour 2 — Interview questions out loud:" },
      { type: "bullet", value: "What happens when you SELECT without an index on 1 million rows?" },
      { type: "bullet", value: "What is a covering index and when would you create one?" },
      { type: "bullet", value: "What does EXPLAIN type=ALL tell you? How do you fix it?" },
      { type: "bullet", value: "Explain optimistic vs pessimistic locking — when to use each" },
      { type: "bullet", value: "What isolation level for a payment system? Why?" },
      { type: "warn", value: "If you cannot answer confidently, do not move to Week 6." },
    ],
  },
};

export default WEEK5_TOPICS;
